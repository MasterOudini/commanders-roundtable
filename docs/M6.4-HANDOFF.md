# Session handoff — build M6.4 (the card-script library)

Paste this whole file as your opening prompt, or say
*"read `H:\Claude Apps\commanders-roundtable\docs\M6.4-HANDOFF.md` and start M6.4"*.

---

## Your task

Build **M6.4a — the pipeline, on easy cards** of Commander's Roundtable, at
`H:\Claude Apps\commanders-roundtable`.

M1–M5 are complete. M6.1 (a bot takes a seat), M6.2 (it plays properly) and
**M6.3 (the engine primitives) are done** — M6.3 was closed on 2026-08-05 against
a restated bar, and the entry in `AGENTS.md` says exactly why the original
wording could not be met. Everything on the pre-M6.4 list is closed (D155, D156,
D157).

**This milestone is the one that moves coverage.** Every primitive M6.3 built
made a class of card *possible* to script. **Two batches are LANDED — twelve
scripts, 1,730 → 1,742, all five gates green in one invocation (M6.4a D158,
M6.4b D159)** — the trigger shapes AND the first activated abilities, with the
`ActivatedDef` seam, a self-sacrifice cost and a computed life cost built on
the way. The job now is the next batches: §8 sizes M6.4a at the first ~200
cards, and the loop below is proven at scale twelve.

## Read first, in this order

1. **`AGENTS.md`** — project rules, the scope-tier model, the milestone log. The
   last entry is *"The pre-M6.4 list, worked (2026-08-05)"*.
2. **`docs/M6.4-LIBRARY-SPEC.md`** — **the authority for this milestone.** It
   supersedes the M6 brief's §6.2. §4 sizes each hard class against the real
   database, §5 is the pipeline, §6 the five gates, §7 the ordering, §8 the
   sub-milestones, §9 what "done" means.
3. **`docs/DECISIONS.md`, entries D155–D157** — the work that exists to make this
   milestone possible. Then skim **D90** (never half-execute), **D122/D124** (an
   unenforced category must be SAID), **D127** (scriptable is not executable) and
   **D147** for the measurement discipline this repo holds itself to.
4. **`scripts/cardgen/README.md`** and **`src/engine/scripts/cards/README.md`** —
   the loop you will run, and what each landed file owes.

## Where the project actually stands

Every number here is measured, not estimated, and reproducible from the commands
in `AGENTS.md`.

| | |
|---|---|
| Cards the engine runs COMPLETELY | **11,231** of 31,692 Commander-legal (M6.4lg, D478 — THE SEARCH ROW RESTORED: `permanent` names no card type, so the search noun expands `Rebel permanent` to one alternative per permanent type (CR 110.4; `nonland permanent` leaves the land out; a Goblin tribal instant is no Goblin permanent card) and folds an alternative spelled with its own `card` (`a basic land card or Gate card`, `up to two basic land cards and/or Gate cards`) to the `or` list; and the row maker admits a search as an ask again - the vocab probe marked it one since D390 while the row maker's ASK_KINDS never did, so every search row had been refused as `effect not a row kind` for ninety decisions although the generator answered searches since D359. ⚠️ THE WIDE LOG TRUNCATES A PAYLOAD AT 77 CHARACTERS; A PROBE'S ASK SET AND THE ROW MAKER'S ARE ONE LIST; THE CYCLING ABILITY SITS UNDER ITS OWN TRIGGER. The yield: 55 rows (the Masques recruiters, the land tutors and fetch lands, the Forerunners ...) and 3 sorceries the parser completes; 23 stale ledger entries retired, 3 added. Prior: 11,173 after D477) |
| Blocked on a per-card SCRIPT and nothing else | **1,781** — of which **0 are OFFERABLE** (the select pool after D478 - the wave rowed 55 by the vocabulary, 23 of them ledgered as searches the suite could not answer in earlier mirrors, retired by the guard; the mirror offered two the wave cannot row - a cycle-head ask and an initiative sorcery - ledgered by reason). Prior: 1,802 after D477 |
| …of those, in the user's own saved decks | **0** — rung 1 is EMPTY since D159 |
| `SHIPPED_SCRIPTS` | **7,417 scripts** (M6.4a–lg), pinned at length 7417 |
| Vitest | **7641 files, 36373 passing / 11 skipped** |
| 500-seed replay fuzz gate | green at **560.0 s wall over 6 shards (D296; 3,600 s per-shard ceiling)** with 7,417 scripts on ROTATING per-seed pools (D193 — CANARY_STAPLES + poolFor; libraries ~600 → ~150; the 60-seed leg fell 308 s → 84 s; rate canaries must be read against the NEW pool shape) on the DERIVED pool (D188 — SCRIPTS/DECK derive from SHIPPED_SCRIPTS; the per-batch fuzz-registration step is DELETED from the loop) (raised THREE times, only ever after a completed-and-equal run proved growth over hang — trend table in the gate's own comment; ⚠️ "self-only def dispatch" is RETIRED (D181): measured FLAT — the bus is at the floor, the wall is the GAMES; ⚠️ NINE rate-canary rots to date: the CR 616 PAIR canary is FIFTEEN copies each after rotting quadratically, and its third rot earns a canary-staples mechanism, not a fourth multiplication, D180) |
| `npx electron scripts/probe.cjs` | 124/124 (against the M6.4lg build) |
| `battery-anim.cjs bot engine prompts` | 141/141 (D442's cleanup-discard check beside D391's proliferate pair; the four walkers answer the discard off the seat's own hand, the may driver off the engine snapshot; reads the bot's commander from the generated deck since D160) |
| Engine fixtures | 8,257 (8,031 by name + 226 tokens) |
| **Gate scheduling** | **Gates wait for IDLE windows** (user directive 2026-08-06): sample load twice ~60 s apart, launch only quiet; light work (classify, write, unit suites, docs) never waits |

⚠️ **11,231 is the only coverage number.** It comes from `engineComplete` and from
nowhere else. Never report a count of scripts written.
⚠️ The bot pool holds its FIRST ENCHANTMENT (`Ajani's Welcome`) and the bot's
curated commander is chosen from the pool — it CHANGES as coverage grows; the
battery reads it from the generated deck (D160).

## What already exists for you

Built in the pre-M6.4 pass, specifically so the first batch lands into a harness
that can fail it:

- **`scripts/cardgen/`** — `select.cjs` (the §7 ordering, offline), `verify.cjs`
  (the five gates, cheapest first), `land.cjs` (registers a drafted script and
  then refuses to claim success). ⚠️ **There is deliberately no `draft.cjs`**: a
  script that called a model would put a network dependency in this repository
  even if nothing shipped it. Drafting is you, writing reviewed TypeScript from
  `batch.json`.
- **`src/engine/scripts/cards/`** — where `land.cjs` writes. Empty but for its
  README, which lists the five things every file there owes.
- **`src/engine/conformance.test.ts`** — five known-hard interactions with
  published answers (CR 613.7c timestamp, 613.8a dependency, 616 ordering, 613.6
  ability removal, 509.1b a granted keyword reaching combat). **A corpus failure
  blocks a release.** Every case asserts both directions.
- **The fuzz-pool guard** in `fuzz.node.test.ts` — every shipped script must be
  registered there *and* dealt in its `DECK`. This repo has broken that rule four
  times (D102, D107, D108, D121) and it is now a test.
- **`SHIPPED_REGISTRY` vs `NO_SCRIPTS`** — one constant until D156.
  `SHIPPED_REGISTRY` is what `host.ts` runs; `NO_SCRIPTS` is empty forever and is
  for tests. Product code may never import `NO_SCRIPTS` (asserted).
- **`sourceIntegrity.node.test.ts`** — no source file may contain a control
  character, and it pins at **nine** the number of test files CI cannot cover.
- **`.github/workflows/ci.yml`** — see the warning below.

## ⚠️ CI is green on less than you think

Nine test files need the 86 MB Scryfall card database, are written with
`describe.skipIf`, and therefore **skip on a machine without it, leaving the run
green.** CI holds types, build, source integrity, the conformance corpus, the
unit suite and the 500-seed fuzz gate. It does **not** hold §6 gate 1's database
half or gate 5 (the coverage accounting) — those run locally:

```
node scripts/cardgen/verify.cjs --full
```

That command, green, is what must be true before a batch lands. `AGENTS.md`'s
own §8 amendment records this rather than pretending otherwise.

## The loop

**The loop is PROVEN — M6.4a ran it end to end** (D158): eight scripts landed,
`verify.cjs --full` green, all five gates. Batches can scale now, but
⚠️ **classify every selected card against what the engine CONSULTS before
drafting it** — `select.cjs` emits every sole-need-`scriptable` card, and a
card whose only text is an activated ability is unlandable until the
`ActivatedDef` seam is built (D158's headline reportable; it cost 4 of the
first batch's 12).

```bash
node scripts/cardgen/select.cjs 25
```

Then, per card in `scripts/cardgen/batch.json`:

1. Read the card's **real oracle text** from the local database —
   `node electron/cardsvc-worker.cjs --query "<name>"`. Never a paraphrase, never
   from memory. This is D15b's rule and it has caught real drift.
2. Write `src/engine/scripts/cards/<moduleName>.ts`, exporting one
   `<MODULE_NAME>_SCRIPT`.
3. Write its test beside it, asserting on **EVENTS** rather than final state where
   possible.
4. Add the card to `fuzz.node.test.ts`'s `SCRIPTS` **and** its `DECK`.

```bash
node scripts/cardgen/land.cjs <moduleName>
node scripts/cardgen/verify.cjs --full
```

**Every landed card owes, in the same commit:** its `tier3.ts` note silent,
`engineComplete` accepting it, a place in the fuzz pool, and its tests. All four
are enforced by tests rather than by convention — `shippedScripts.node.test.ts`
and `fuzz.node.test.ts` between them.

## Non-negotiable constraints

- ⚠️⚠️ **Never use `git stash` or `git restore`.** The tree carries ~90 files of
  uncommitted work sitting on an old HEAD. Undo by editing, or by string surgery
  against a verified marker.
- ⚠️ **The generator may never touch `src/engine/`** outside `scripts/cards/` and
  the one `SHIPPED_SCRIPTS` line that imports from it. Primitives are hand-written
  reviewed work — that is where correctness for *every* card is decided, and a
  generated change to a primitive is a change to every card at once.
- **Scripts return EVENTS.** They are never handed a mutation API. `ctx.random` is
  a seeded scratch RNG threaded through the log, so a coin-flip card replays
  bit-exactly.
- **A script that cannot be verified is not landed.** No exceptions, no "mostly
  works" tier.
- **Never half-execute** (D90). A card the parser understands only in part must
  not run its understood half silently — that is the one outcome worse than not
  running it at all.
- **An unenforced category must be SAID** (D122/D124). Silence in this app means
  "handled"; 16,020 cards once said nothing at all.
- `src/engine/` is pure and deterministic: no React/Electron/Node/zustand, no
  `Date.now`/`Math.random`/`performance.now`. `src/bot/` additionally may not
  import a `GameState`-taking engine module or read `process.env`.
- **Offline-first is absolute.** Approved exceptions are listed in `AGENTS.md`.
  ⚠️ §7 names popularity data as an internet dependency — **stop and ask the user
  before fetching any.**

## Traps this repo has already paid for

Each of these cost real debugging time. They are here so you do not buy them
again.

- **`preview_start` does not work with these Electron apps.** Drive the renderer
  with `npx electron . --dev --remote-debugging-port=9223
  --disable-backgrounding-occluded-windows --disable-renderer-backgrounding`,
  then `node scripts/cdp.cjs "expression"`.
- **`window.__crt.engine.view()` lags the engine by one animation group** (D137,
  D146). It catches up only while the game is FLOWING; at the moment it stops it
  stays wrong. Assert off `submit()` results or the DOM.
- **Never run the batteries and the 500-seed gate at the same time, and never
  read a wall-clock number off a loaded machine** (D106). Three phantom
  regressions in this project, one of which triggered a four-hour hunt.
- ⚠️ **Patch scripts: write them to a FILE and run `node <file>`.** The shell eats
  backslashes inside `node -e`; that is how three regexes were written with a
  literal BACKSPACE instead of `\b` and **silently disabled three architectural
  guards for twenty-four decisions** (D153, D154). The repo is CRLF and some files
  carry mixed endings — normalise before anchoring.
- **No backticks inside a template literal** in `battery-anim.cjs`.
- **A "same multiset of lines" check does not prove a document reorder was safe**
  — every wrong arrangement is also a permutation (D157's correction).
- **The battery's step name is `precombatMain`, not `main`.**
- **`[data-instance-id]`, not the `[data-hand-instance]` slot wrapper**, and
  passing priority through a hotseat moves the viewer (D119, D145).
- **A prompt's ANSWERERS and its CONTROL are separate work** (D143). "The bot,
  the fuzzer and the net driver can answer it" reads exactly like "it is
  finished", and has twice not been.
- **A GATE THAT HAS ONLY EVER RUN WITH A SEAM SWITCHED OFF HAS NOT TESTED THE
  SEAM** (D368). A walk gated on `grantIndex.size > 0` costs nothing for
  exactly as long as no shipped script carries a `gt` def - so the decision
  that ships the FIRST one is the first gate that can see it, and D368 paid
  for it with a bot suite at 437.9 s and 5 of 6 failing. When a new walk
  joins an existing bus, give it the gates its neighbours already have (a
  per-event-KIND index, D162; a present-def memo, D168) rather than one
  emptiness check.
- **A HEREDOC THROUGH THIS HARNESS HALVES BACKSLASHES** (D369). A doubled
  backslash arrives as one and a backslash-b arrives as a literal BACKSPACE -
  invariant 14 in a patch script rather than a regex - and one D369 applier
  mangled a type import that way before `tsc` caught it. An applier never
  spells a backslash: it writes the currency sign and `patchlib.cjs`
  substitutes `String.fromCharCode(92)` at runtime. A single Bash command
  past ~100 lines is TRUNCATED besides (`unexpected EOF`) - split payloads
  into part files and concatenate them.
- **`advanceUntil` ANSWERS EVERY PROMPT IT PASSES, and `settle` IS an
  `advanceUntil`** (D370). A fire that ends in a settle answers its own
  question - the harness `simplestAnswer` declines - unless the settle`s own
  predicate already holds the moment the prompt goes up. D370's sacrifice rows
  passed because it did (the ability had left the stack) and its counter rows
  went red because it did not (the opponent`s spell was still on it). A
  scaffold that waits after raising a prompt waits for the PROMPT OR the quiet
  board, never for the quiet board alone.
- **A fixture a generator DERIVES is in no names list** (D370). A wave adds its
  own card names and its carried `helpers.json`; a cost fixture the row maker
  derived (D347) is in neither, and the suite says so only at run time. Read
  every name a generated suite puts or deals against the fixture WANTED list
  before the port.
- **A GRANT IS NOT ALWAYS A KEYWORD - the derive keeps THREE carriers apart**
  (D371): a keyword on `chars.keywords` (a Set), a landwalk TYPE on
  `chars.landwalk` (a string[] `canBlock` reads, CR 702.13) and a protection
  QUALITY on `chars.protection` (D356 gave it a structure). A reader, a guard
  or an emitter that knows only the first is silently wrong for the other two:
  the row maker's own `hasGrant` asked whether the keyword list was non-empty
  and emitted NOTHING for Goblin King, whose whole grant is mountainwalk.
- **THE PROBE AND THE CLASSIFIER MUST AGREE, and the probe is the one that
  lies** (D371). A candidate probe reads a scope head with a regex of its own
  where the classifier reads it with `staticRowShape`, so a widening applied
  to one and not the other leaves cards the pool OFFERS and the probe calls
  "not candidates" - which reads exactly like a missing card rather than like
  two readers disagreeing. Widen both, in the same pass.
- **THE DISCLOSURE EXAMPLE THAT KEEPS GOING SILENT** (D371). `tier3.test.ts`
  proves that a static the engine does not run is SAID even when the keyword
  it grants is enforced, and it has lost every example it has had to the
  engine growing - Avacyn (D300), Emblem of the Warmind (D324), Akromas
  Memorial (D371). Replace it with a grant the row grammar CANNOT express,
  never one it merely has not expressed yet.
- **A MANA ABILITY IS NOT A DEF** (D372, CR 605). It never uses the stack, so
  the engine carries it as the derived LIST `chars.producesMana` - what
  `manaSourcesOf`, `legalActions`, `tapForMana`, the payment plan and the
  client preview read - and NOT as a def the offer loop resolves. A granted
  one is therefore a list entry `pushGrantedMana` appends at derive time with
  `abilityIndex = producesMana.length`, never a `<provider>#g<n>` ref, and a
  reader that routed it through `activatedDefFor` would half-run it (D90).
- **TWO GRANTS ON ONE PERMANENT ARE TWO PRODUCTIONS** (D372). Metallic Sliver
  under Cryptolith Rite and Basal Sliver carries index 0 (auto-tappable) and
  index 1 (priced - D325 keeps the solver off it); the first draft of the
  seam test expected ONE and was the thing that was wrong. Assert the list,
  never its length.
- **A DOUBLED BACKSLASH HALVES IN A MEASUREMENT SCRIPT TOO** (D372). The
  row-kind count read 41 because an escaped digit class arrived as a bare
  letter inside a template literal; `[0-9]+` gave the honest 49. Every script
  that passes through the harness spells no backslash, not only an applier -
  and a shard leg's `CRT_FUZZ_OUT` is a FILE path, not a directory (EISDIR
  after a green 60-seed replay).
- **THE SELF WORD IS NOT THE SELF SUBJECT** (D373). Twice now a decision has
  predicted that a quoted payload needs its self word NORMALISED and been
  wrong: `damage` has always read "This creature deals" as its source, and
  `selfRef` has always spelled a card own name `~`. What a payload like
  "This creature gets +1/+0 until end of turn." needs is an effect whose
  SUBJECT is the resolving object own source, which is a parser rule AND an
  aim in `effects.ts` - measure the vocabulary with the real parser before
  calling a payload family a wave.
- **`it` IS NEVER A PARSER SUBJECT** (D373). On a spell "it" is the PREVIOUS
  sentence target ("Destroy target creature. It can't be regenerated.",
  "... Untap it."), so a rule admitting it reads that sentence for the spell
  itself - a whole card going `auto` over a clause aimed at the wrong thing.
  A QUOTED body "it" IS its recipient, so the rewrite belongs in
  `scripts/vocabulary.ts`, where the text is KNOWN to be a quoted body.
- **A HEAD LIBRARY IS A TAIL; A FILTER IS A MECHANISM** (D374). 109 head
  texts the library did not hold were 1.54 cards each - a tail no list of
  entries finishes - and by BASE EVENT they collapsed to seven. Read a head
  the library does not hold as a BASE head plus a CLOSED filter on its
  subject, and keep the BASE head NAME: the generator keys a dozen lists on
  it, and a filter that narrows an existing name inherits all of them.
- **PRICE A BUCKET WITH A THROWAWAY THAT CARRIES ON** (D352, D374). The row
  maker stops at the first blocker, so `trigger head not in the library: 168`
  says nothing about how many cards the head is the ONLY blocker for. A
  throwaway copy that accepts any head answered it: rows 171, unrowable 53.
- **THE FIXTURE A GENERATOR DERIVES IS IN NO NAMES LIST** (D347, D370, D374).
  A filtered head fires only for a card the filter admits, so the suite deals
  a DERIVED one - and it is in neither the wave own names nor helpers.json.
  The port collects them AND sweeps every name every generated suite deals
  against WANTED before it runs.
- **A GENERATED MATCHER MAY READ NEITHER PARAMETER** (D374). A filter naming
  neither the source nor its controller emits a body mentioning neither `ctx`
  nor `self`, and an unused parameter is a `tsc` error: name the parameters
  from the BODY, at the generator (D267).
- **THE FIXTURE TEST PINS THE TOKEN COUNT BESIDE THE TOTAL** (D374). The
  port sed repins only the total, so a wave with a new token pin must move
  the token count too.
- **A SEAM THE ENGINE ALREADY HAS CAN BE REFUSED BY THE SUITE** (D375). Ten
  cards sat in the ledger under `a vocabulary effect the suite cannot assert:
  regenerate` while the ENGINE had run regeneration since D330. When a class
  is refused for what a TEST cannot do, the work is a generator arm and not a
  seam - read the refusal string, not the class name.
- **A SHIELD ASSERTED ALONE IS HALF THE RULE** (D375). Asserting that a
  regeneration shield exists proves the ability resolved and nothing about
  what a shield IS. Destroy the shielded permanent and assert it SURVIVED,
  tapped and undamaged, with the shield spent - and pick the killer from what
  the permanent IS, because no creature-removal spell can hit an artifact.
- **A BLANKET GUARD OUTLIVES ITS REASON** (D375). The fixture reader refused
  every artifact-, enchantment- or land-creature clause outright, while
  `satisfies` had long since learned to check `cardTypes` and Metallic Sliver
  had been in the list since D371. Narrow a guard to the cases the reader
  below it genuinely cannot serve, and let that reader refuse the rest by
  name.
- **A FILTER NARROWS A BASE AND CANNOT INVENT ONE** (D376). When the filtered
  heads run out, group what is left by BASE EVENT and take the densest base
  with an event the library already has - every base added is a base the
  filter then narrows for free, which is the compounding half.
- **A BLOCK NAMES TWO CREATURES** (D376). The head is about the row own and
  the filter is about the other one, so the conjuncts are read off the other
  side of the block - `b.attacker` for a `blocks` head, `b.blocker` for a
  `becomesBlocked` one.
- **A FIXTURE WITH A JOB TO DO IS NOT THE PLAINEST CARD** (D376). A block
  fixture has to attack or block, so it needs power, toughness and no
  defender - and never a LAND creature, because Dryad Arbor prints nothing,
  wins every tie-break, and is read by any row that counts lands.
- **A DERIVED FIXTURE MUST RIDE THE ABILITY** (D376). The row maker carried
  the filter and not the gift, so every suite attacked with the Cyclops and
  the trigger would have fired for nothing. READ a generated suite before the
  port; do not wait for the port to fail.
- **A REQUIRED FIELD HAS A BOUNDARY, AND IT IS THE DOWNSTREAM COUNT** (D377).
  D355 and D356 make a new field REQUIRED so `tsc` names every construction
  site, and that is right until the sites that must say NOTHING outnumber the
  emitters by three orders of magnitude: 1,836 shipped card modules construct
  a `CardsMoved` and none of them is a sacrifice. Then the honest instrument
  is a test that DRIVES every emitter plus a canary with a floor - and say so
  where the field is declared, or the next reader will think the rule lapsed.
- **A CANARY IS NOT ALWAYS THE HASH** (D377). D364 added one because
  `poolSnow` is in the state hash and 500 equal replays would have proved
  nothing about an empty field. `CardMove.reason` never reaches `GameState`
  at all, so the hash cannot vouch for it in either direction: what the floors
  prove is that REAL GAMES produce the values, which is a different claim and
  the only one available.
- **A HEAD IS ONLY LANDABLE IF A SUITE CAN FIRE IT** (D377). Read the printed
  head, then ask what one submit would make the engine do it. Every sacrifice
  in this engine is a cost behind a def gate except a mana ability whose price
  is the permanent own sacrifice, which needs no script at all - so that is
  the fixture the derivation looks for, and a subject no such card satisfies
  is refused BY NAME rather than tested on something the suite cannot spend.
- **A ROW FIRES ITS OWN HEAD** (D377). A card that watches for a sacrifice and
  also has an activated ability that sacrifices does BOTH in that ability own
  test, and the baseline is what is wrong. Give the head contribution to the
  PAYING ability rather than widening the assert by hand.
- **A COMMENT IS AN EDIT** (D377). The self-audit corrected a figure inside a
  comment in `src/engine/types/events.ts` while the gate unit suite was
  running. Nothing behavioural moved and no assertion could have read it - and
  a gate log is a statement about a TREE, so the gate was stopped and restarted
  from the top. Twelve minutes is the whole cost of keeping a gate honest.
- **A CHECK WHOSE OUTPUT YOU PIPE CANNOT FAIL YOUR SCRIPT** (D377). The
  close-out ran `npm run build`, the probe and the battery through `| tail -3`,
  so `set -e` was reading the exit code of `tail`. The battery went red AFTER
  the push. Read `PIPESTATUS[0]`, or write a log and abort on a non-zero exit.
- **RENAME THE TEST, NEVER THE CHECK** (D377, and D144 and D147 before it). The
  battery greps `src/` for the word prompt followed by a bracket, to catch a
  `window.prompt` call that throws in Electron. Three test NAMES have tripped it
  now. It is the check working.
- **A ZONE THAT ANSWERS THE QUESTION NEEDS NO SEAM** (D378). D377 had to build
  `CardMove.reason` because a sacrifice and a discard are invisible in the zones.
  A card LEAVING a graveyard says so in its own `from` and a card PUT INTO one in
  its own `to`, so both bases are a head regex and a matcher with nothing under
  `src/engine/` moved. Ask what the EVENT already says before designing a field.
- **A CARD IN A GRAVEYARD HAS NO DERIVATION** (D378, and D171 before it). Read
  its type off the ORACLE FACE, and narrow the filter to what an oracle face can
  answer - card types and subtypes - rather than reading `derive` for a permanent
  that is not there. And a BARE subject needs no oracle read at all: emitting one
  and then returning true leaves a binding nothing uses, which is a `tsc` error.
- **A DERIVE SCRIPT IS EXEMPT FROM ITS OWN RENAME** (D378, D354 verbatim). The
  sed that produced `derive378.cjs` rewrote its own `.split(gen77-)` FROM side, so
  it no longer quoted what it was meant to find and every internal require in the
  derived chain stayed one decision behind.
- **A HEAD REACHED BY DROPPING A CHECK MUST BE PROVEN ON SOMETHING ELSE** (D379).
  The four bases this decision opened are the library head with its `=== self`
  test removed, so a suite that fires the trigger on the row own card passes with
  the test still in place - D128 green tick over nothing, in the one shape a
  widening can take. Fire it on a FIXTURE, from the OTHER seat where the printed
  line says any player, and on the exit no narrower head already catches.
- **A FILTERED HEAD KEEPS THE BASE NAME, SO EVERY LIST KEYED ON THAT NAME MUST
  ASK WHETHER IT IS FILTERED** (D379). That is D374 whole point and its cost: the
  board adjustment claimed the row own card had left, the cleanup target sat on
  the turn the fire itself ends on, and the untap hand and library adjustments
  belong to the SELF head alone. Sweep the trigger name with a COUNT (three sites
  for the untap, where an applier anchor found two).
- **RECOGNISE ON THE SCRUB MASK, CUT FROM THE RAW LINE** (D380, and D366 before
  it). The compound splitter finds its joining word on a mask that blanks every
  quoted span IN PLACE with spaces of the same length, so an offset found there
  still points into the raw text. A quoted ability carrying the word `or` would
  otherwise be split down the middle - and the mask is what proves it is not.
- **A PRONOUN BELONGS WHERE BOTH HALVES ARE IN HAND** (D380). `it` in the second
  half of a compound names whatever the FIRST half was about, and the row maker
  own rule is that `it` names this permanent only under a head about this
  permanent. Resolving it in the SPLITTER keeps the head library from learning a
  word whose meaning depends on a line it cannot see.
- **TWO DEFS ON ONE CARD NEED TWO IDS AND TWO TITLES** (D380). A compound can put
  the same head on one card twice (Ultron Auxiliary watches its own graveyard
  from two zones): two defs sharing a ref is two abilities the engine cannot tell
  apart, and two tests sharing a title is a failure that cannot say which half
  broke. Disambiguate ONLY when it must, so every id shipped before stays
  byte-identical.
- **A MODIFIER ADDED TO ONE PUSH IS DROPPED BY EVERY OTHER** (D381). The three
  PAIR heads (attacksOrBlocks, isDealtDamage, entersOrAttacks) push their two
  abilities and continue BEFORE the general push at the foot of the trigger
  branch, so a condition added to that one alone vanishes for them - and a card
  that does MORE than it says is the one direction D90 forbids outright. Put
  every modifier in ONE object and spread it into EVERY push.
- **A RULE THE ENGINE CANNOT REACH FROM ITS EMITTERS BELONGS IN THE FUNNEL**
  (D382). Hundreds of shipped modules build a `DamageDealt` themselves, so a
  prevention shield consulted inside `effects.ts` would be skipped by every one
  of them. CR 615.1 says a prevention effect IS a replacement effect, and the
  funnel is the one place every event passes through - correct by construction
  rather than by a list of call sites kept in step. And walk the WHOLE BATCH:
  `applyReplacements` sees one body at a time against a state that does not
  advance between them, so two damage events would each spend the same shield.
- **A BLANKET GUARD OUTLIVES ITS REASON** (D382, D375). The clause fixture picker
  refused any clause whose kinds included `battle`, and `any target` is
  [creature, player, planeswalker, battle] - so every prevention row was refused
  for a clause a CREATURE serves perfectly. Narrow it to the case that genuinely
  has no fixture; read the refusal STRING, never the bucket label (D365).
- **AN APPLIER MUST MATCH THE FILE ITS OWN LINE ENDINGS** (D382). A heredoc
  payload is LF and `triggers.ts` is CRLF, so the anchor matched NOTHING. The
  counted guard caught it - which is the difference from D258, where a silent
  `String.replace` over a CRLF file reported success - but catching it every
  time is not handling it: convert the payload to the FILE`s own line ending.
- **A SWEEP CATCHES EVERY CREATURE ON THE BOARD, INCLUDING THE ONES THE ARM
  PUT THERE** (D383). A row whose card is a creature is inside its own `each
  creature` scope, and so are the five Goblins its tap cost just tapped - both
  read a card in the GRAVEYARD with its battlefield fields cleared, so the cost
  mark reads false. Ask the scope arithmetic of EVERY subject the suite asserts
  on, from ONE reader, or a fix reaches one and misses the other.
- **INVARIANT 14 BITES A MEASUREMENT SCRIPT TOO** (D383). A doubled backslash
  halves in a heredoc, and inside `new RegExp(string)` the JS string escape then
  eats what is left: a digit class became the letter `d` and the sweep measured
  1 card where the honest figure is 12. A seam priced at a twelfth of its worth
  would not have been built at all - use regex LITERALS and explicit ranges.
- **A WAITER WHOSE PREDICATE CANNOT MATCH IS AN INFINITE SPIN** (D383). Three
  background waiters were found still polling after SIX HOURS: two were
  `until grep -qE ...` loops over a log whose wording never contained any
  alternative (a battery that prints `Failures:` matches neither `FAIL` nor
  `failed`), and the third had CRASHED hours earlier and hung on a trailing
  `node -c` - which, with no file argument, reads STDIN and never returns. They
  were not idle: each spawned a `sleep` and a PowerShell every 15-20 s, on the
  machine, while a gate ran - which is the load D106/D255/D270 blame for
  spurious timeouts. Grep for a string the tool ACTUALLY prints, prefer a cheap
  log grep over a process poll, keep ONE waiter per thing, and use
  `node --check <file>`, never a bare `node -c`.
- **A DEF CLAIMS ONE LINE, SO COUNT THE PRINTED ONES** (D384). `lineClaims`
  matches a def is text against ONE unaccounted line (D158), so a def whose
  text is the WHOLE printed face silences nothing - and the row maker is
  multi-line flag was counted off the row is LEFTOVER lines, which are what the
  accounting has not claimed rather than what the card prints. Five modules
  landed, registered, typechecked and passed their own suites while leaving
  every one of their cards INCOMPLETE; `shippedScripts.node.test.ts` named all
  five, and nothing else noticed. Count PRINTED lines.
- **`settle` DOES NOT ANSWER A PROMPT ITS PREDICATE ALREADY SATISFIES** (D384).
  A staged activation puts nothing on the stack until its targets are chosen,
  so an empty stack with no pending triggers holds WHILE a `chooseTargets`
  awaiting is up: `settle` returns at once, the answer never arrives and the
  cost is never charged (the log ends `CastBegan, AwaitingSet`). D370 is trap
  from the other side - there a settle ATE a prompt by answering it, here one
  walks past by not trying. A scaffold that waits after raising a prompt waits
  for the prompt to be GONE (`awaiting === null`), never for the quiet board.
- **WHERE A COST MARK CAN BE READ DEPENDS ON WHETHER THE PAYLOAD TARGETS**
  (D384). CR 601.2 charges a staged activation on the ANSWER (D169), so a cost
  mark taken before the targets are declared reads the board as it was; an
  untargeted ability is charged at the submit and the same read is correct.
  One rule, two placements - decide it from the PAYLOAD, never from the cost.
- **A BY-NAME SACRIFICE IS NOT A COST, AND THE CAPITAL LETTER IS THE TELL**
  (D384, D377). `Sacrifice Ninja is Kunai` names the CARD, so the carrier is own
  guard throws at MODULE LOAD rather than claim it - refuse the row at the
  generator or the whole batch fails to import. ⚠️ A guard reading `Sacrifice`
  plus a word is too broad: `Sacrifice two other creatures` is a COUNTED
  sacrifice D353 charges. The discriminator is the CAPITAL letter.
- **A NEGATIVE MUST BE OUTSIDE THE LINE, DECIDED FROM THE KIND, THE SOURCE AND
  THE RECIPIENT TOGETHER** (D385). A source restriction or a combat kind is what
  a spell's noncombat spray at the recipient proves; an all-damage `to and dealt
  by` line covers the recipient from EVERY source, so its negative is damage at
  something ELSE. The first cut sprayed the enchanted creature under Heart of
  Light and the spray was, of course, prevented - a negative that fires inside
  the line proves the line by failing.
- **AN OPPONENT'S INSTANT WAITS FOR THE OPPONENT'S PRIORITY** (D385). Light of
  Sanction's negative is the opponent's Spark Spray at my creature, submitted
  while p1 held priority - refused, and `must` said so. A helper that casts for
  p2 passes priority to p2 first.
- **STATICS BEFORE SHIELDS, IN THE ONE FUNNEL** (D385). A continuous prevention
  effect absorbs the whole entry at no cost, so a consumable shield asked first
  would be spent on damage that was never going to land; both shapes live in
  `prevention.ts` inside the replacement funnel (CR 615.1) and `effects.ts`
  consults neither - the tripwire `prevention.node.test.ts` pins both facts.
  The registry's `preventions()` accessor is the funnel's GATE (D368): a seam
  gated on an empty registry is free until the first def, and the first def is
  the one that pays.
- **`BlockerDecl` CARRIES `attackerOrder`, NOT `attacker`** (D385). A blocker
  may block several attackers, so `creatures blocking it` reads
  `b.attackerOrder.includes(self)`, never an equality.
- **AN UNUSED HELPER IS A `tsc` ERROR - EMIT IT ONLY WHERE A FIRE OR AN ASSERT
  USES IT** (D385, D267). Five suites' worth, all fixed at the generator; a
  generated helper nobody calls fails the whole batch's typecheck.
- **A HEREDOC PAST ~83 LINES IS TRUNCATED MID-STRING** (D385, invariant 14).
  `unexpected EOF` on a generator whose source read perfectly; the suite half is
  two part files, and the whole generator spells no backslash (`[.]` for a
  literal dot, `String.fromCharCode(10)` for a newline).
- **A CANARY STAPLE'S FUEL IS A CLAIM ABOUT WHAT THE *DRIVER* PRODUCES, NEVER
  ABOUT WHAT THE CARD DOES** (D385, D193). D385's first staple was `Fog Bank` -
  "a 0/2 flying defender that BLOCKS, so the fuzzer's own attacks run into it",
  which reads perfectly and was an assumption about the driver that nothing had
  checked: `declareBlockers` answers `blocks: []`, so THE FUZZER HAS NEVER
  BLOCKED IN THIS WHOLE ARC and the canary read ZERO over 500 seeds with every
  other stage green. Before declaring a staple, find the line in the driver that
  produces its fuel; if you cannot point at one, the canary is a red tick over
  nothing (D128 inverted). ⚠️ And the CONSEQUENCE stands as its own reportable:
  every `BlockersDeclared` head, `AttackerBecameBlocked` and every prevention
  effect that fires only in a block is covered by unit tests ALONE.
- **A DOUBLED BACKSLASH IS HALVED BY THE HARNESS BEFORE THE SHELL SEES IT -
  QUOTED HEREDOC OR NOT, PYTHON OR NODE** (D386, invariant 14). A Python
  applier whose search string spelled one backslash the Python way (doubled)
  received it SINGLE, and a single backslash before `u002f` is the character
  `/`: the replace meant for four escapes hit all 53 slashes in the file. A
  single backslash survives; a doubled one does not; and an applier never needs
  either - spell it `String.fromCharCode(92)` from a node FILE, and COUNT
  backslashes with `split(String.fromCharCode(92)).length - 1`, never with a
  grep that has to spell one. This entry's own first draft halved while
  describing the trap.
- **A HAND COUNT IS PROVEN BY REMOVAL, NEVER BY `put(…, 'hand')`** (D386,
  D232). `put` finds a copy the opening hand already holds and moves it hand
  to hand - net zero - so Empyrial Armor read 10/10 where 11/11 was expected.
  Move two cards the hand is known to hold to the graveyard, then one back.
- **A COMPUTED STATIC IS PROVEN BY A READ THE COUNT CAN MOVE** (D386, D300).
  Compute the starting count in the GENERATOR from the arm's known board and
  assert the base absolutely; then each counted fixture must move the derived
  P/T by exactly the printed delta, and one leaving must take it back. A suite
  that reads one board passes with the count hard-coded.
- **WHAT `other` EXCLUDES DEPENDS ON THE SENTENCE - EXCLUDE BOTH ENDS** (D386).
  Bravado's "other creature" excludes the ENCHANTED creature; Ancestral Mask's
  "other enchantment" excludes the AURA ITSELF. `countOf(ctx, self, applied)`
  skips both: excluding a non-matching object is a no-op, so it is right in
  every placement with no guess and no refusal.
- **AN ASKING PAYLOAD'S FIRE STOPS AT THE PROMPT** (D389, D370). A vocabulary
  trigger whose payload is a library look must not end in `settle`: a settle
  while a SPELL is still under the trigger (the second spell of the turn, the
  heroic Growth) answers the prompt through the harness and walks on, and the
  arm's own wait then runs the game out. Strip the trailing settle for an asking
  row the way the targeted rows do; the arm's `advanceUntil` is the wait.
- **THE LOOK'S KEEP IS STAGED ON TOP BEFORE THE FIRE, EVERY DEALT COPY** (D389).
  A look reveals the top N at RESOLUTION and the answer may only name those. A
  single staged copy is taken by any draw step the fire crosses; two are dealt
  (outside the deck's Set, D365) and every copy in the library or the hand goes
  on top.
- **THE LOOK'S HAND PROOF IS LOCAL TO THE ANSWER** (D389, D232). `put()` takes a
  witness out of the opening seven when it holds one, and a row-level
  `hand0 + N` assert then reads one short for a reason that has nothing to do
  with the look. Read the hand right before the answer and assert `+ picks`.
- **A CONST INSIDE THE FIRE'S BLOCK IS NOT AT `armed()`'S SCOPE** (D389). The
  fire lines sit in an `if (which === n) { … }`; a value the return statement
  carries must be declared before the block and assigned inside it - or, as
  here, not carried at all.
- **AN ENTERS-OR-ATTACKS LINE IS TWO DEFS ON ONE LINE TAG** (D389, D318). A
  per-ability variable keyed on the line alone is pushed twice; key it on the
  trigger too (`varOf`'s shape).
- **AN AMPERSAND AFTER AN AND-LIST BACKGROUNDS THE WHOLE CHAIN** (D389). `cd X
  && cp … && (…) &` runs the `cd` in the job, not in the shell that follows, and
  every later command in the same line runs in the old directory; a repin
  chained the same way ran invisibly. One waiter per thing, absolute paths.
- **THE SELECTOR PRINTS NOTHING WHILE ITS PINS ARE RED** (D389). `select.cjs`
  runs the select suite and stops on its first failure; the pool it computed is
  in `scripts/cardgen/batch.json` regardless - read it there.
- **THE FIXTURE PICKER'S LEGALITY GUARD READ A FIELD THE DATABASE DOES NOT HAVE**
  (D389, red 1). `gen89-fixture.cjs` skipped `commanderLegal === false` where the
  record says `commanderLegality` (`legal` / `not_legal` / `banned`), so
  `undefined === false` never fired - dead since D347 - and the derivation picked
  Little Girl (Unhinged, `{HW}`, mv 0.5) and Dance of Panic as look fixtures.
  `oracleParse`'s mana-value pin caught the first at the gate. The guard reads the
  database's own field now; Gobland (D360) came through it earlier and is reported,
  Emrakul (D356) is a deliberate boundary. Sweep every fixture name against
  `commanderLegality` when a picker is touched.
- **A BATTERY BLOCK SETS THE VIEWER, WALKS PRIORITY TO ITS SEAT BEFORE FUNDING A
  CAST, AND READS THE DOM WHILE A PROMPT IS UP** (D389 follow-up; D145, D146, CR 500.4).
  The look-panel block SKIPPED in the close-out's run and recorded the skip as a PASS -
  `startSolo` left the viewer on p2 so p1's hand read `card: null`; mana funded before
  p1 held priority emptied at the step boundary; `view().awaiting` read null under a live
  prompt. A precondition failure is a FAILURE, never a skip, when the deck makes the
  precondition certain.
- **A RESOLUTION THAT ASKS SEVERAL PLAYERS IS A QUEUE ON THE STATE, NOT A LOOP IN
  THE RESOLVER** (D390). `effectEvents` stops at the first `AwaitingSet`, so a
  second player's question cannot be raised from the same resolve; `pendingAsks`
  carries who is still owed one, the answer handler continues the queue, and the
  batch lands when the last has answered. A player with no choice is never asked
  (D137's shape) - record the forced pick as the queue is built.
- **AN ASKING PAYLOAD UNDER A COMBAT-DAMAGE HEAD KEEPS A WALK TO THE PROMPT OR
  POSTCOMBAT** (D390, D259). The generic combat walk had been stripped from every
  asking row, so a queued discard on a connect trigger stopped in the
  declare-attackers step and read "expected undefined". The quiet predicate needs
  `pendingTriggers.length === 0` beside the empty stack.
- **AN INLINE WANTED SWEEP CAPTURED A TEST TITLE AS A CARD NAME** (D390). A regex
  over a generated suite for `put(g, 'p1', '…')` reads `test('…')` too. Sweep the
  suite's OWN names list, never its text.
- **THE VEIL'S HITS ARE RENDERED BY AN EFFECT AFTER THE VEIL DIV** (D390). A
  battery block that reads `[data-aim-legal="1"]` the instant `[data-aim-veil]`
  appears reads none. Wait for a hit, not for the veil.
- **THE TABLE MODE OUTLIVES THE GAME** (D390, D144). A block whose error path
  leaves a mode armed poisons every section after it: the prompt bar shows the
  mode's copy over the next game's prompt and the face chooser never opens.
  `setGameSetup` resets the mode and the aim on every game start now; a block
  still answers what it raised and escapes in its `finally`.
- **TWO IDENTICAL PERMANENTS ARE ONE SLOT AND ONE HIT** (D390, D112). A veil over
  two Grizzly Bears has one legal hit; put a DIFFERENT card on the board for the
  negative (a Swamp, offered dark) and expect one pile, not two cards.
- **READ THE BOARD AFTER `table.settle`, AND THE DOM BESIDE THE VIEW** (D390,
  D146). `view()` lags one animation group and stays wrong once the game stops
  after a click; the graveyard read "false" on a pick the DOM already showed.
- **A PIN THAT LIVES IN TWO PLACES MOVES IN TWO PLACES** (D390, D267). The bot
  pool's enchantment count is asserted twice in `botPool.node.test.ts`; a repin
  script that knows one site leaves the other red. Grep for the OLD value after
  every repin.
- **A 60-SEED FUZZ LEG OUTRUNS A 600 s FOREGROUND TIMEOUT** (D390). At 5,445
  scripts the leg alone passes ten minutes; run it in the background and read its
  log, or the harness kills a green run.
- **SBA 8 ANNIHILATES +1/+1 WITH -1/-1** (D391, CR 704.5q). A test that puts a
  +1/+1 counter beside a -1/-1 to prove that every kind grows reads `{}` at the
  next check. A second KIND on one creature is a charge counter.
- **AN EFFECT KIND MUST NEVER SHARE A NAME WITH A PROMPT KIND** (D391, D357). The
  producer guard scans `src/data` for `kind: '…'` constructions, so an effect
  named like the prompt counts the parser as a producer: `proliferate` the
  effect, `proliferateChoice` the prompt.
- **A DERIVED CLOSE-OUT SCRIPT CARRIES THE PREVIOUS DECISION'S ANCHORS** (D391).
  A repin derived from the last decision quotes THAT decision's FROM side. Write
  the FROM side from the live pins the previous decision LEFT, and expect pins it
  never moved to move (the `counter` primitive and the bot pool's lands did).
- **NOTHING IN THE CHAIN WRITES A HELPERS FILE** (D391, D390). D390's was
  hand-written; a derived port that waits on one stalls on a file no step
  produces. The sweep over the suites' OWN names lists is the proof of what the
  WANTED list is missing.
- **THE VEIL'S `active` LIST IS A SECOND MODE REGISTRY** (D391, D144). A new table
  mode needs its targets in `GameLayer`, its toggle in `aimCommit`, its copy in
  `PromptBar` AND its name in `AimVeil`'s `active` prop, or the board stays dark
  under an armed prompt. A battery block's cleanup answers UNCONDITIONALLY -
  `view().awaiting` lags the engine, and a gated cleanup left the ask open and
  the mode re-arming after every Escape. A backtick in a comment inside the
  `js()` template literal ends the literal.
- **AN ASK THAT IS NOT THE LAST SENTENCE IS THE CONTINUATION SEAM** (D391, D195,
  D389). `Proliferate. Draw a card.` (Steady Progress, Contentious Plan) is refused
  by design, not by accident; do not widen the ask-last rule to land it.
- **A LATER SENTENCE ABOUT THE PREVIOUS TARGET IS READ BY THE EXPLICIT RULE, AIMED
  WHERE THAT CLAUSE AIMED** (D392). `it` / `that <noun>` at the head of a sentence,
  or as the object of untap / tap / destroy / exile / sacrifice / return / attach,
  is rewritten to the previous clause's phrase in the text handed to the rules
  ONLY; the clause keeps its printed text, so the targeting layer's count and the
  effect count still agree, and `parseEffects` aims it at `nextTarget - 1`. Never
  add a referent to a rule's own regex: the rewrite is the one place.
- **COUNT THE STRUCTURE, NOT THE FAMILY** (D392, D384). Threaten, the regenerate
  clause and half of fight priced as three seams by family; by sentence structure
  they were ONE gap (563 one-piece cards carry a referent sentence), and the
  cheapest seam on the board was a parser-only one. A pricer by exact shape hides
  a shape that runs across families.
- **A COUNTER AND A PUMP ON ONE FIXTURE ADD UP** (D392). The generator's P/T
  expectation read the pump alone, so a two-clause payload (a +1/+1 counter, then
  the same creature granted a keyword) expected the base and the engine rightly
  read one more. The deltas are summed over the whole payload before a line is
  written.
- **A NEGATIVE PIN IS CHECKED AGAINST THE RULE TABLE FIRST** (D392). "Its
  controller draws a card." has a rule of its own and read `auto` before the seam;
  a refusal pinned on it failed for the wrong reason. Pin the boundary as "read by
  its own rule, never rewritten" and refuse a sentence no rule holds.
- **THE SUPPRESSION PREDICATE IS THE RETIREMENT SIGNAL** (D392, D187, D295). A
  parser seam that reads a hand-scripted spell's whole text turns its predicate red
  in the gate; retire the def with D295's tool (`retire-N.cjs` - the registry is
  mixed-EOL: split on LF, keep each line's CR) and let the vocabulary run the card.
  A pin that says a sentence is never read describes a meaning the seam changes:
  rewrite it (D117).
- **A CONTROL CHANGE WITH AN END IS AN ENTRY ON THE UNTIL-END-OF-TURN LIST, AND
  THE REVERT IS AN EVENT THE CLEANUP STEP EMITS** (D393, CR 514.2). The reducer
  cannot emit, so `ControlChangedUntilEndOfTurn` lands the change and remembers
  `controlRevert` on the same list the pumps and keyword grants use (power 0,
  toughness 0 - inert at every layer `derive` reads); the cleanup step emits one
  ordinary `ControlChanged` per remembered entry, newest first, before
  `UntilEndOfTurnEnded` clears the list. CR 302.6 rides the same event: the
  permanent is summoning-sick under its new controller, and the printed haste is
  what lets it attack.
- **THE ENGINE HAS NO OBJECT STAMP** (D393, CR 400.7). A permanent that leaves the
  battlefield and comes back in the same turn keeps its instance id, so the
  cleanup revert is gated on "still on the battlefield" and nothing finer. A
  stolen creature bounced and recast by its owner inside one turn would be
  handed to its previous controller at cleanup - the named reportable, not a
  surprise; an entered-on stamp on `CardState` is the fix when a card needs it.
- **PRICE A FAMILY BY WHAT ITS TAIL IS** (D393, D384). The fronted duration
  ("Until end of turn, target creature gets ...", 80 one-piece lines) lands 5
  under a normalization - the rest carry a scrubbed keyword; the activation
  restrictions ("Activate only as a sorcery.", 313 lines) are blocked by their
  PAYLOADS (59 parse), not by the restriction. Both were measured with the real
  parser and neither was built. A shape count is not a landing count.
- **THE GENERATOR'S ASSERTABLE SET IS A SECOND VOCABULARY** (D393, D391). A new
  effect kind the vocabulary reads is refused by the row maker until the
  generator can ASSERT it ("a vocabulary effect the suite cannot assert:
  control"); the arm is one case in `assertLines` and one name in `ASSERTABLE`,
  and the fixture side comes from the clause's controller word (no word means an
  opponent's).
- **A RESTRICTION WITH AN END IS A FLAG ON THE UNTIL-END-OF-TURN LIST, READ WHERE
  THE RULE IS DECIDED** (D394, CR 509.1b, CR 514.2). "Can't block this turn"
  rides `PtModifiedUntilEndOfTurn` as `cantBlock` (power 0, toughness 0 - inert
  at every layer `derive` reads), `canBlock` refuses the blocker with a reason of
  its own, and cleanup clears it with the pumps. No new event, no new list.
- **A CLAUSE'S PHRASE IS ITS TARGET FIRST, THE SELF ONLY WITHOUT ONE** (D394, D392).
  "~ deals 2 damage to target creature. That creature can't block this turn."
  starts with the self marker and is about the creature dealt the damage; the
  first cut read the referent as `~` and the second clause fell silent. A referent
  after a COUNTED clause carries the same optional mark and runs over the same
  picks ("Those creatures can't block this turn.").
- **A RESTRICTED-ONLY BOARD IS A PROMPT WITH NOTHING IN IT** (D394, D119). With
  the only potential blocker restricted, the engine declares no blockers itself
  and never asks; a test that waits for the blockers prompt to read the refusal
  must give the defender a second, unrestricted creature.
- **PRICE THREE NEIGHBOURS BEFORE THE PICK** (D394, D384). Bite + fight priced at
  20 landings, the untap skip at 15, the can't-block restriction at 37 - by
  dropping or standing in the sentence and asking the real parser. The pick was
  the honest count, not the family size.
- **AN ANIMATION IS THE SAME ENTRY THE PUMPS RIDE, READ AT FOUR LAYERS** (D395, CR
  613.4b). "This land becomes a 3/3 green Ape creature with trample until end of
  turn." is one `PtModifiedUntilEndOfTurn` with `basePt` (layer 7b, before the
  Tier-3 override), `types` and `subtypes` (layer 4), `colors` (layer 5 - SET, not
  added) and `keywords` (layer 6); cleanup ends it with the rest. "It's still a
  land." beside it is a `noop`. No new event, no new list.
- **COUNT THE CAPTURE GROUPS, OR NAME THEM** (D395). The animate rule's build read
  the keywords from the artifact group and the artifact word from a keyword group;
  every parse pin failed at once and the in-play land had no flying. A rule with
  more than four groups gets a comment listing them - or named groups.
- **THE STALE-REFUSAL GUARD IS THE OTHER HALF OF A SEAM** (D395, D187). A class the
  seam BUILDS turns its ledger rows stale ("Hydroform - land animation", "Vivify -
  until-end-of-turn type change with P/T set"); the guard names them and they are
  DELETED, never kept for the record - the entry's own text is the record.
- **PRICE BY THE HONEST COUNT, TWICE** (D395, D384). The delayed action at the next
  end step priced at 142 by structure and 12 by the parser (token copies, "another
  target" and "you may ... if you do" hold the rest); the animate family at 121 and
  38 (+ the fronted form). The parser's count picked.
- **THE ENGINE INDEXES ACTIVATED ABILITIES, NOT PRINTED LINES** (D395, D371). The
  line probe carried an Equip line's PRINTED index; an Equipment whose Equip
  follows another activated line (its own animation) has its Equip at 1, and the
  generated suite activated index 2 and was refused "no such ability". The probe
  reads the engine's own activated list for the index now.
- **THE LINE PROBE RUNS BEFORE THE PORT, NEVER AFTER** (D395). Re-run after the
  wave had landed, it read every rowed card as complete, dumped no lines, rowed
  nothing, and emptied the names and modules lists the close-out reads; they
  were rebuilt from the module files. A probe that reads completeness describes
  the tree it runs on.
- **TWO OPERANDS ARE TWO INDICES ON ONE CLAUSE** (D396, CR 701.12). A bite or a
  fight names its subject (a target, the self, or the referent) and its OTHER
  target (`otherTargetIndex`, a second index `parseEffects` hands out after the
  subject's, in printed order); the executor resolves the object through the
  same `picksFor` / `aimOf` the subject went through, and emits ONE `DamageDealt`
  with one or two entries through `damageTo`, so deathtouch, lifelink, infect and
  wither ride along. Either operand gone or not a creature at resolution is no
  damage at all (CR 701.12b/c), said aloud.
- **A DERIVED GENERATOR RENAMES ITS OWN COMMENTS** (D396, D362). An anchor that
  quotes the previous decision's comment ("D395 - an animated permanent ...")
  matches nothing in the derived copy, which already says D396. Quote the
  renamed text, or anchor on code.
- **THE PREDICATE NAMES THE RETIREMENTS; THE OLDER SUITES WITHOUT IT ARE A LATENT
  DUPLICATE** (D396, D187, D295). Thirteen hand fight and bite scripts went red on
  the suppression predicate and retired; twenty-two others (Pounce, Savage Smash
  ...) stayed green only because their suites predate the predicate test. A script
  the vocabulary reads whole still runs by the script - harmless, and a reportable.
- **A COST IS READ OFF THE CARD, NOT REMEMBERED** (D396). Two in-play proofs paid
  one mana for a {1}{R} and a {1}{G} spell and were refused `cannotAfford`; the
  harness says so plainly, and the fix is one number, but the number comes from
  the fixture's text.
- **THE GENERATOR PROBES ITS OWN ASSERTS WITH A NULL ROW** (D396). `needsKw` and
  `needsBoard` render the assert lines once with no row to decide which helpers
  the suite declares; an assert arm that reads the row's own base without a
  guard crashes the whole generation with "reading 'base'" three frames away
  from the arm. Guard the row in every self-aimed assert.
- **A RESTRICTION IS ENFORCED AT ONE SEAM, AND THE SEAM IS THE INPUT** (D397, CR
  106.4). `fitFor` subtracts every pool bucket and drops every source whose
  restriction the purpose does not fit BEFORE the solver runs - the three tiers
  and the min-cost max-flow are untouched, exactly as D364's snow reservation
  left them. A purpose is CHARACTERISTICS, never a card (the client previews from
  a PlayerView, D53): a devoid face is colourless, a face-down spell is a
  colourless creature with no subtypes (CR 708.2), an ability's purpose is its
  SOURCE'S derived type line and colours. The default purpose is `other`, which
  fits nothing - the SAFE direction: a caller that forgets is under-permissive,
  never over.
- **A LINE HELD CONDITIONAL FOR ONE REASON HIDES A MISCOUNT FOR ANOTHER** (D397,
  D90). `Add six {G}` (Castle Garenbrig) read as ONE green since the parser was
  written, unreachable while the line's "only" kept it conditional; the moment the
  restriction was enforced the card would have landed at a sixth of its mana.
  Price with the real parser AND print the productions of every line the seam
  will claim - a shape count is not a landing count, and a landing count is not
  a correctness check.
- **A DEDUPE KEY THAT DOES NOT KNOW THE NEW FIELD DROPS THE NEW LINE** (D397,
  D116). `parseManaProduction` dedupes a face's productions by tap, condition,
  amount and cost, and a restricted line identical in everything but its
  sentence to an unrestricted one on the same face (`{T}: Add {C}.` beside
  `{T}: Add {C}. Spend this mana only to cast an artifact spell.`) was DROPPED
  as a duplicate the moment its condition matched - eleven lines, found by the
  parse report's `manaAbility` pin FALLING by eleven where a seam can only raise
  it. A field that makes two abilities different belongs in the key.
- **AN ANY-COLOUR TAIL THE PARSER CANNOT NAME MUST REFUSE, NOT WIDEN** (D397,
  D116, D90). `Add one mana of any color among legendary permanents you control`
  fell through to scope 'all' since D116 - five colours off an empty board - on
  Mox Amber, The Grey Havens and Plaza of Heroes, because the refusal was keyed
  on the two words `could produce` and not on the tail being non-empty. Found
  because the seam made Plaza of Heroes OFFERABLE and its printed lines were
  read one by one. Every tail the parser cannot resolve to a set is unread now;
  the three "among ..." scopes are a named widening.
- **A SUB-POOL IS A SECOND LEDGER THE PAYMENT MUST NAME** (D397, D364).
  `poolRestricted` sits beside `poolSnow`, both bounded by `pool` (asserted); the
  reducer cannot infer which bucket a spend drew on, so `ManaSpent.restricted` is
  REQUIRED (one emitter) and the handler spends the FITTING buckets first, the
  general mana last. `ManaAdded.only` is OPTIONAL for D377's reason: two dozen
  ritual scripts construct a plain `ManaAdded` and none is restricted; the fuzz
  canary keeps the one emitter that must fill it honest.
- **A DERIVE'S FILE LIST IS A ROT SITE** (D397, D378). The memory applier's
  next-anchor file was NOT carried into d397 and the applier would have died on a
  missing file at the last step of the close-out. Check every file an applier
  reads EXISTS and still matches the tree it anchors on before the gate, not
  after.
- **THE SWEEP NAMES THE DERIVED FIXTURE; THE PORT MUST FEED IT BACK** (D397,
  D347, D370, D374, D375). `sweep-deck-names.cjs` found Big Bertha - the Hero
  keep Avengers Tower's look arm DERIVED - missing from WANTED, wrote it to
  `sweep-missing-names.json`, and the port regenerated the fixtures without
  reading that file; one suite failed on "no fixture card named". A sweep that
  only REPORTS is a check whose result nothing consumes: the port adds the
  missing list through `add-wanted-json` before it regenerates, and the count
  it prints is the proof.
- **A MULTI-FILE PART TEXT NEEDS ITS OWN DRIVER** (D397, D377). `patchlib.cjs`
  takes one part file per repo file; a seam that touches nine files is nine
  counted applies - `apply-parts-d397.cjs` splits one text on `@@FILE@@` headers
  and applies each section counted, so the swap count is still exact per file.
- **THE RED SET'S SHAPE IS THE DIAGNOSIS** (D398). A port's first run at 30 red
  where the reds are EXACTLY one row kind (the 30 conditioned enters-with rows)
  and 0 of the other names the arm before a line of the log is read; grouping
  `^ FAIL` by suite and by title costs one command and settles where to look.
- **A TWO-STAGE ARM'S RE-ENTRY IS THE FIRE'S OWN CAST** (D398, D256). Stage one
  casts the card with the record empty, proves the replacement did not apply,
  sends it back to hand and plays the recipe; the fire then casts it again. A
  manual move as the re-entry leaves the fire casting a card already on the
  battlefield (`wrongZone`, 29 suites), and a stage-one check on a card that
  never entered is a green tick over nothing (D128).
- **A RECIPE THAT CASTS N SPELLS DECLARES N VARIABLES** (D398). `const castC`
  once per spell was a duplicate declaration at N = 2; suffix by index and
  settle between casts, because a sorcery needs an empty stack.
- **THE TURN RECORD IS FED BY THE EVENTS, NOT BY THE LIFE EVENTS ALONE** (D398,
  D348). `applyDamage` moves life without a `LifeChanged`, so a `lostLife` fed by
  life events had never seen combat damage; `recordDamage` reads the damage
  events (poison excepted, lifelink's gain beside it).
- **A DERIVE THAT RENAMES EVERY MENTION ROTS THE PROVENANCE** (D398, D378, D396).
  `derive398.cjs` turned every "D397" in a chain file's COMMENTS into "D398";
  rename file-name tokens and the header line only (derive399).
- **A CANARY'S CONDITION MUST BE ONE THE DRIVER MEETS, MEASURED** (D398, D385).
  A staple picked for its cost is a green tick over nothing if its condition
  needs combat: the driver declares 97 real attacks against 2,948 empty ones in
  60 seeds (a random subset over every untapped permanent, refused whole when
  one pick is illegal), so Raid, Bloodthirst and the lost-life forms sit at
  zero over a 500-seed gate. Probe the rate at 60 seeds with a throwaway copy
  of the fuzz test that writes to a file, and anchor the condition at the START
  of the caster's turn (an upkeep trigger: Drana's Emissary for a drain, the
  Mantra for a gain) when the driver's own play cannot reach it.
- **THE PAYLOAD HARVEST PEELS THE CONDITION** (D398). A line probe that hands the
  row maker a trigger payload with its intervening if still attached makes
  every conditioned payload refuse as "not a pump"; harvest the peeled payload
  beside the whole line, in both capitalisations.
- **A RIDER ON A PAYLOAD THE ROW MAKER ALREADY READS IS A WIDENING, NOT A KIND**
  (D399). Six refusals "trigger payload not a pump" on ONE wording (`~ gets +1/+0
  until end of turn and can't be blocked this turn`) name the reader, not a new
  row kind: the `SELF` regex takes the rider as an optional group, the row carries
  a flag, the generator's emitter spreads it onto the event and the assert reads
  the until-end-of-turn list. Widen at the row maker BEFORE the port.
- **`parseEffects`'s THIRD ARGUMENT IS `isInstantOrSorcery`** (D399). `false`
  returns `manual` before a rule is tried, so a parse test of a permanent's own
  line (`This creature can't be blocked this turn.`) must pass `true`, or the
  form is refused for a reason that has nothing to do with the rule under test.
- **THE ENGINE ASKS NOBODY WHEN NO LEGAL BLOCK EXISTS** (D399, D232/D234). A lone
  unblockable attacker draws no `declareBlockers` prompt at all; a refusal by
  name is proven only with a second, blockable attacker beside it.
- **AN OPTIONAL EVENT FIELD IS SPREAD CONDITIONALLY** (D399). The reducer copies
  `cantBeBlocked` onto the entry only when the event carries it, so every pump
  without the rider is shaped exactly as before; a REQUIRED field on the parser's
  `EffectSpec` with one constructor is the cheapest guard the other way (a rule
  that forgets it fails to compile).
- **THE FIXTURE BY-NAME COUNT IS `T - K - 7`** (D399). Seven helper exports sit
  beside the cards in `engineCards.ts`; the port's sed reads all three numbers off
  the regenerated file and never off a draft.
- **THE EMPTY-RECORD MARK IS TAKEN ON THE STAGE TURN** (D400, D398). An
  intervening-if arm that marks the stack on turn 3 and walks to turn 5 passes
  turn 3's OWN end step, where the card's entry that turn may meet the condition
  (a creature entered; a card left your graveyard, because the arm enters the
  card from the graveyard) - the trigger fires there CORRECTLY, and a mark taken
  before the walk reads it as "fired with its condition unmet" (three suites).
  Take the mark on the stage turn's main phase, after the entry turn's step.
- **A `you may pay` PAYLOAD IS THE PAYMENT PROMPT'S SENTENCE** (D400, D369). The
  row maker's optional peel strips `you may` from a trigger payload (CR 603.5)
  and must leave `you may pay {M}. If you do, ...` whole, or the vocabulary is
  handed `Pay {2}. If you do, ...` and refuses it as "not a pump" (Markov
  Purifier, Liliana's Devotee; Ghostly Pilferer in D399).
- **THE ABILITY-WORD LIST IS ONE LIST, MIRRORED** (D400). The classifier reads
  past any `Word — ` prefix; the row maker reads past `TRUE_ABILITY_WORD` only,
  so a word outside it (Skyswarm) is offered and then refused as "a head not in
  the library". Add the word to the row maker when the classifier's prefix
  admits it.
- **A CHAINED SCRIPT CARRIES A STALE NAME THROUGH A WHOLESALE RENAME** (D400,
  D399). `port400b.sh` still called `gen-oneshot98.cjs` after two derives (the
  rename touches the decision number only) and expected a names-before file no
  step had written; read a chained script before its first run in a decision.
- **A RECIPE THAT NAMES A TURN IS A RECIPE FOR ONE ARM** (D400). The attack
  recipes in `gen100-cond.cjs` said `turnNumber === 3`; the stager takes the fire
  turn from its caller (`fireTurn`), so a refire row's recipe walks to turn 7.
- **A HEREDOC PART FILE HALVES ITS BACKSLASHES TOO** (D401, invariant 14). A
  regex spelled `'\d'` in a part file the applier reads arrives as `'\d'`,
  which a TS string reads as `d` - tsc-green and silently wrong - and a repair
  regex that spells `\.` matches ANY character (it doubled `\d` into `\.d`).
  Grep the target file after every heredoc; spell a backslash by character code
  in a fixer, never in a regex literal.
- **A MIRROR WITH AN `i` FLAG IS NOT A MIRROR** (D401). The reader's subtype is
  `[A-Z][a-z]+`, case-sensitive; the classifier's copy under `i` admitted `you
  control a token`, and its keyword list admitted a protection the threshold
  body refuses. The two sets are ONE set: no flag, keywords alone, and a wording
  the classifier admits and the reader refuses goes to the ledger by name.
- **THE STAGE THAT MEETS A CONDITION MAY FIRE THE CARD'S OWN HEAD** (D401). A
  `ManualSetLife` that raises the total is a life GAIN; a gain-life head on the
  same card answers it with a counter that moves the P/T the static's proof
  reads (Twinblade Paladin). Refuse the pair at the row maker.
- **A DRAFT-TIME PULL AFTER THE PORT OWES FIVE THINGS** (D401): the registry's
  import and entry, the WANTED name, the two files, the fixture pins (re-read
  `T - K - 7` off the regenerated file) and the names-before file the re-port
  check compares against.
- **THE ROW MAKER'S ABILITY WORDS ARE THE CLASSIFIER'S PREFIX** (D401, D400
  again): Infusion joined Skyswarm.
- **THE HARNESS AUTO-PASSES THROUGH THE UPKEEP** (D402). A game test that waits
  for `s.turn.step === 'upkeep'` never sees it unless the game holds everywhere
  (`holdEverywhere(g)`, as every generated suite does): the trigger fired and
  resolved inside one `PassPriority`, the predicate was never true, and the probe
  read turn 47 with the game finished by decking.
- **THE FIXTURE'S APOSTROPHE IS CURLY** (D402). `turn's` in a regex reads the
  test's literal and never the printed text (`turn’s`); a delay regex, like any
  reader of printed prose, takes both apostrophes.
- **`self` ON A DRAW MEANS THE CONTROLLER** (D402). The parser marks a draw or a
  life gain `self: true` with no self AIM; a guard that refused every self effect
  refused the cantrips. Refuse the self aim of a `SELF_AIMED` kind and the self
  sacrifice, nothing else.
- **"SACRIFICE IT" AFTER "CREATE A TOKEN" IS NOT A SELF SACRIFICE** (D402, D369).
  The rules read the sentence as `sacrificeSelf` (D369's body), and a delayed
  wrapper that accepted it would have armed the spell eating ITSELF at the next
  end step - the referent (the token) is what the sentence names, and it is
  refused by name until an entry carries the ids its clauses created.
- **A SELF-SACRIFICE COST IS PAID ONLY BY A DEF** (D402, D159). A permanent whose
  delayed token sits behind `Sacrifice this creature` runs as a ROW and never on
  the parsed path (`notCastable` at the handler); the engine test proves the end
  step's timing at the bus, over the reducer, with a synthetic entry, and the
  cantrip's whole path with Blessed Wine.
- **THE TOKEN-KEY READER SWALLOWS A DELAY INTO THE KEYWORDS** (D402). `Create a
  1/1 white Spirit creature token with flying at the beginning of the next end
  step` keyed `flying at the beginning ...` and refused as outside TOKEN_TABLE; a
  payload carrying `at the beginning of` falls to the vocabulary.
- **A GENERATED `const` NOBODY READS IS A tsc ERROR** (D402, `noUnusedLocals`).
  The delayed block declared three baselines and read one; declare only the
  baselines the asserts read.
- **A HEREDOC PATCH FILE HALVES ITS BACKSLASHES** (D403, invariant 14 - three
  times in one decision). A regex spelled `\{` in a patch string arrived as
  `{`; a `split('\n')` arrived as a literal newline inside the quotes and
  broke the TS it wrote. EVERY backslash in a patch file is the currency sign
  (patchlib's `¤`), and a fix that must spell one composes it from a character
  code - never a heredoc, never a `node -e` string.
- **SHELL BACKTICKS EAT A `node -e` EDIT** (D403). A memory edit whose text
  carried backticks had them command-substituted by bash and wrote two empty
  spans; prose with backticks goes through a script FILE.
- **A SPELL'S KEYWORD COST IS NOT A CLAUSE** (D403, D306/D307). With the Kicker
  line among its sentences the face read `assisted` and the kicked draw never
  ran; the line is dropped before the clauses decide the mode, beside Cycling
  and Flashback.
- **THE HARNESS AUTO-PASSES A SPELL TO RESOLUTION INSIDE ONE SUBMIT** (D403,
  D402). Assert the kick on the `SpellCast` event's object, not on a stack that
  is already empty when the submit returns.
- **A KICKED REPLACEMENT READS THE MOVE, NOT THE STATE** (D403). The funnel
  runs before the entry is written, so the permanent's `kicked` is empty at
  that moment; the emitter reads the move's and declares no helper and no
  context (tsc refuses an unused one).
- **THE ROW MAKER READS ONLY THE LEFTOVER LINES** (D403). A line the engine
  claims (the Kicker line now) never reaches it; the kick mana is read off the
  card's own oracle text, reminder text stripped.
- **A CAPITALISED WORD AT THE LINE START IS NOT A SUBTYPE** (D404). `predicateOf`
  places an unknown capitalised word as a subtype - right mid-sentence (`a
  Forest`, `a Mount`) and WRONG for a line's first word: `Colorless spells`,
  `Face-down creature spells` and `Noncreature spells` read as subtypes no face
  carries, the reduction never applied and the coverage claimed the line. The
  true-leftover DIFF (D389's rule: read the names) exposed it; a reader whose
  subject starts the line refuses the non-subtype words by name, and a subtype
  vocabulary at parse time is the honest fix (a reportable).
- **A BACKGROUNDED COMMAND WITH AN INNER `&` DIES WITH ITS SHELL** (D404). A
  regression run launched as a backgrounded subshell inside a backgrounded
  command was killed when the outer shell returned - one line of log. Background
  the command itself.
- **THE RITUAL IN THE OPENING SEVEN** (D404, the generator). A fixture put into
  the hand INSIDE a fire block moves hand-to-hand when the shuffle already dealt
  it, and every count taken before the block is one short; the fixture goes in
  before the baseline (the `secondSpell` shape) and `castsFromHand` counts the
  cast.
- **A NEGATIVE ANTHEM KILLS THE 2/1 EEL** (D404, the generator). A -1/-1 read off
  a toughness-1 fixture is read in the graveyard as its absence (CR 704.5f); the
  2/2 Bears carry it.
- **THE DRIVER'S FALLBACK IS NOT A CANARY** (D405). Paying with convoke / improvise /
  delve only when the mana fell short reached a pick eight times in twenty seeds
  and cast none - the pick is one usable action among many. The driver pays with a
  payable pick ALWAYS, as it always kicks (D403).
- **A STAGED CAST REFUSED AT ITS PAY STAGE IS A WEDGE** (D405). The harness answers
  a targets prompt with a legal pick or a cancel, never a cancel after a
  `cannotAfford`, so a cast whose remainder the pool cannot pay is answered
  forever (HEAD's seed `leak` already wedged at intent 200 this way). The driver
  proves a mana plan for the remainder BEFORE it casts; the leak game then plays
  all 300 intents (10,818 events, 41 permanents, 47 s alone) and its budget moved
  to 180 s on a COMPLETED run (D370's rule).
- **THE SECOND COLOURED SYMBOL IS THE WALL, AGAIN** (D405; D398, D403). A
  `{3}{G}{G}` convoke staple read ZERO over 60 seeds with a green creature paying
  one `{G}`: the other needs a second green source in a core with one Forest a
  seat. A staple's coloured part is ONE symbol.
- **A CONVOKED MANA CREATURE IS NO MANA SOURCE FOR THE SAME CAST** (D405). The
  solver saw the Elves as a source and would have tapped it twice; the plan is
  solved without the tapped permanents (`solveWithout`) and a plan that names one
  is refused (`planTapsAlt`).
- **A HEREDOC PATCH FILE PAST ~100 LINES TRUNCATES** (D405, invariant 14's
  neighbour). The handlers patch and the fuzz patch each split into a driver and a
  PART FILE of lines (`alt-3-helpers.cjs`, `fuzz-d405-parts.cjs`).
- **THE BUILD'S TYPECHECK IS THE ONE THAT COUNTS** (D406). `tsc --noEmit -p
  tsconfig.json` passed a `legal.ts` that `tsc -b` (the port's and the build's
  check) refused for an unimported type; a whole decision (D405) had used the
  narrower check. Check with `tsc -b`.
- **A HEREDOC HALVES AN ESCAPED QUOTE TOO** (D406, invariant 14). `\'s` in a
  ledger reason arrived as a bare quote inside a single-quoted TS string and
  broke the file's transform; a reason with an apostrophe is emitted as a
  DOUBLE-QUOTED string by a small builder (`ledger406-parts.cjs`), never spelled.
- **A LIVE `turnNumber` IN A WALK PREDICATE NEVER MATCHES** (D406). A predicate
  reading `g.state.turn.turnNumber + 1` moves with the walk and runs the game to
  its end (decking); capture the turn before the walk.
- **A RESPONSE NEEDS THE HOLDS** (D406). Without `holdEverywhere` the answerer's
  priority is auto-passed and the spell on the stack resolves before the response
  can be cast.
- **THE PICKS OF A CAST COST COME BEFORE ITS TARGETS** (D406, the UI). The
  `sacrifice` / `costPick` modes run first and the picks ride the targeting mode
  into the review; a spell whose targets should inform the sacrifice is a
  reportable, not a bug the host can see.
- **THE SECOND WALL IS THE NOUN LIST** (D407). The until-leaves rider read at
  once for `target creature an opponent controls` and for nothing else the
  family prints - the vocabulary's nouns are a CLOSED list, and the target
  parser's controller reader had made the missing entries safe to add long
  before. Price a family's TARGET PHRASES, not its rider alone.
- **A TRIGGER'S TARGET PROMPT IS NOT A CAST** (D407). `CancelPendingCast` on an
  enters trigger's prompt is `noPendingCast`; answer it (the harness's
  `simplestAnswer` shape) or aim it.
- **A HEREDOC PATCH HALVES `\n` INSIDE A JS STRING** (D407, invariant 14 again).
  The vocab generator's patch emitted a template with REAL newlines where `\n`
  escapes were meant - the same output by luck; a part file or the currency
  sign, always.
- **THE FLICKER WITHIN ONE BATCH** (D407). The linked exile's return is a
  state-based check, not the leave's own aftermath: a permanent that leaves and
  re-enters in ONE batch keeps its stamp moving, and the return still fires
  (the stamp differs) - but a return that CR 610.3c wants "immediately" waits
  for the next state-based check here. Reportable, not a bug the gate can see.
- **THE PARSER'S CASE** (D408). The oracle prints `You may pay 4 life rather
  than …` at the head of a line and `you may …` after an ability word or a
  condition - a reader anchored on one case reads half the family. Probe the
  FAMILY's sentences before the regex is trusted.
- **A `Pick<>` IS THE SHAPE TWO READINGS SHARE** (D408). The additional cost
  and the alternative cost carry the same five verb fields and nothing else in
  common; `CostVerbs` lets D406's `costPicksProblem` charge either without a
  cast between the types.
- **ONE PICK STAGE PER CAST** (D408). A face whose additional cost has a
  chooser verb gets NO alternative reading - the client's cost-pick stage is
  one, and a second would wedge the staged cast (D405's lesson, again).
- **A RESPONSE-WINDOW STAPLE IS THIN FUEL** (D408). Daze alone read ONE
  alternative cast over 60 seeds (a spell on the stack, an Island, the pick);
  Mistvein Borderpost and Snuff Out - sorcery-speed, no target window - took
  it to 11. A canary's staple must be castable in a main phase.
- **A SEAM'S YIELD CAN BE WHOLE CARDS, NOT ROWS** (D408). Claiming one line
  completed 29 cards with no script and drained twelve ledger rows; the wave
  was 2 rows. Read the coverage number, not the pool.
- **A SYNTAX CHECK IS `node --check`, NEVER `node -e "require(...)"`** (D408). A
  require RUNS the driver: the next decision's applier went into the tree while
  this decision's gate was open, and came back out through its exact inverse
  (every swap reversed, the copies unlinked) before the close-out - the gate's
  shards had loaded their modules long before, so its verdict stood. Write the
  inverse before the driver is needed; check syntax with `node --check`.
- **A RE-DERIVE OVERWRITES THE DRAFTS** (D409). `derive409.cjs` was re-run after
  the generator had been patched and the patch was silently gone - the row maker
  refused every row until it was re-applied. Derive ONCE, then patch; a deriver
  is not idempotent over patched files.
- **A LATE HEAD WALKS PAST THE QUESTION** (D409). A combat-damage fire advances
  to the postcombat main and the HARNESS answers the explore's question on the
  way (keep on top); a proof that waits for the prompt reads nothing. Branch on
  whether the question is still up, and read the harness's keep otherwise.
- **DOUBLE STRIKE FIRES A COMBAT-DAMAGE TRIGGER TWICE** (D409, CR 702.4b). Jenny,
  Generated Anomaly explores twice; the generator's `strikes` rule must reach
  every proof under that head, the vocab proofs included.
- **THE CLASSIFIER NEVER READS "it"** (D409). `it explores` under a self head was
  `unclassified` while `this creature explores` was `scriptable`: the one-shot
  shapes name the heads under which "it" is the permanent (D303), and a new
  self-aimed verb needs its own shape there.
- **THE ROW MAKER'S ASK LIST IS CLOSED** (D409, `ASK_KINDS`). A new asking kind
  is refused as "trigger payload not a pump" until it is listed, and the vocab
  probe must register the `~`-spelled key the row maker looks up.
- **THE PIN TEST READ ONE QUOTE** (D409). `tokenPins` matched `tokenRef('…')`
  and the generated rows print `tokenRef("…")`: three shipped rows (Gargoyle
  Flock, Lord Dregg, Regal Caracal) created tokens whose printing the fixture
  oracle never held - a nameless 0/0 the state-based action bins - unseen until
  this decision's pool rotation dealt one in a seed of the 500 and the union's
  `tokensNamed === tokensCreated` canary caught it (the gate's second run). The
  three are pinned (fixtures 6,461, 153 tokens) and the test reads both quotes.
- **A TYPED CYCLING LINE MADE THE SPELL MANUAL** (D410). The D306 clause drop
  read `Cycling {N}` only, so six spells with a readable body sat in the pool as
  "a spell with a line outside the vocabulary" naming their OWN body. A
  synthesized ability's line must leave the spell's clauses wherever its plain
  form does.
- **THE PORT HAD NO TOKEN-PIN STEP** (D410). The row maker has written
  `token-pins-needed-dNNN.json` since D400 and nothing ever applied it: D400's
  three tokens were the nameless 0/0s of D409's first gate, and this wave's
  Dinosaur was caught by the widened pin test before the gate.
  `add-token-pins.cjs <json>` runs in the port before the fixtures are built.
- **THE SEARCH PROMPT LISTS THE WHOLE LIBRARY** (D410). The candidates are the
  SET (D61) and the type is enforced at the answer; a proof picks a matching card
  and may prove the refusal of one that does not match.
- **THE HAND OFFER NAMED ONLY THE CARD** (D410). Two typed cyclings on one card
  read the same in the offer; the label carries the type now.
- **THE OLD SEAM'S TEST PINNED THE OLD BOUNDARY** (D410). `cyclingSeam.test.ts`
  asserted that a landcycling is NOT synthesized and stays `keyword:altCost` -
  the D306 boundary, now the opposite by design - and the gate's unit suite
  caught it twenty minutes in. Run the neighbouring seam's own suite before the
  gate when a seam moves a boundary the last one drew.
- **THE OLD BOUNDARY WAS PINNED THREE TIMES** (D411). `manaDrawback.test.ts`
  (D355) held Thalakos Lowlands incomplete with a null drawback,
  `engineComplete.test.ts` listed it INCOMPLETE and `tier3.test.ts` expected its
  note - the boundary this seam moved, found by running the neighbouring seams'
  suites BEFORE the gate (D410's lesson, applied). The accounting's guard now
  sits on a synthetic rider the engine still refuses.
- **THE OTHER TAP SITE** (D411). A mana line's price (D355's damage) lived at
  the hand tap only: every auto-paid cast that tapped a painland dealt no
  damage. A price charged "in the same accept as the mana" must be charged at
  EVERY site that makes the mana - `manaDrawbackEvents` is the one helper now.
- **THE HALVED `\n` IN A TEST STRING** (D411, invariant 14 again). A `\\n` in a
  heredoc part became `\n` in the applier, which wrote a REAL newline into a TS
  string literal - an unterminated string at transform. Build a backslash-n from
  `String.fromCharCode(92)`; never spell it in a part.
- **A DERIVER DOES NOT KNOW A NEW PORT STEP** (D411). `add-token-pins.cjs` was
  written in D410's folder and never added to the deriver's chain, so the D411
  port failed at its first line; the chain carries it now.
- **`node -e "require(...)"` RAN A DRIVER AGAIN** (D411 - the D408 trap, a second
  time, during this decision's gate): the next decision's fixtures driver went
  into WANTED and came back out through its inverse before the close-out. The
  rule stands and is now absolute: a driver is checked with `node --check` and
  never required; write the inverse beside every driver before it is needed.
- **A STAPLE WITH NO SCRIPT IS A VANILLA BODY** (D412). The 60-seed canary read
  ZERO when it ran before the wave's port: Raffine's Informant was dealt to every
  seat and connived nowhere, because the fuzz deals a staple by NAME and its
  enters trigger is the ROW's. Port first, then measure a staple whose mechanism
  lives in a generated row.
- **THE PROOF STAGES ITS OWN HAND** (D412). `findAnywhere` finds the library
  first, so a card to discard must be moved to the hand before the fire, and a
  nonland to discard must be dealt - the padded library is basics.
- **A REGEX LITERAL TAKES ONE MARK, A TEMPLATE TWO** (D413, invariant 14 again).
  The rider rules written as regex literals carried the currency sign twice - a
  literal backslash and any character - where only the template rows need two;
  three of four rules read nothing until the mark count matched the string kind.
- **THE REFERENT REWRITE SUBSTITUTES EVERY REFERENT** (D413). `If that creature
  would die this turn, exile it instead.` became `... exile target creature
  instead.` - the object `it` too - and the rule had to admit the target phrase
  where `it` was.
- **A DEVOID SPELL WAS MANUAL FOR ITS KEYWORD** (D413). The engine honoured
  Devoid since D310, but a SPELL's clause list still held the line as an unread
  sentence; five ledger rows and three offers said `Devoid` was the wall. A
  keyword line the engine honours must leave a spell's clauses wherever the cost
  keywords do.
- **A ONE-CARD STAPLE ON A TARGETED SORCERY IS THIN FUEL** (D413). Lava Coil
  alone read 3 marks at 60; Anger of the Gods, marking every creature it
  damages, took it to 10.
- **A FALSE-POSITIVE GUARD HID A DEAD BRANCH** (D414). `FP_BEFORE` listed
  `another` since D288, so the count reader's `another` branch never ran and
  `another target creature` read as no clause at all; the tell was
  `vocabularyTargets` returning nothing for a sentence the reader had a branch
  for. A lead word in the false-positive list is a word the reader never sees.
- **THE VOCABULARY'S COUNT LIST IS A SECOND LIST** (D414). `vocabularyEffects`
  refused `Another target creature ...` after the target parser read it -
  `effectParse`'s `COUNTED` lacked `another`; the two lists move together.
- **TWO CLAUSES OF ONE ABILITY MUST NOT SHARE A FIXTURE** (D414). Drooling
  Groodion's generated suite picked the Cyclops for `target creature` and again
  for `another target creature`; the engine refused the second pick. The
  generator's `fixturesOf` takes a fixture once per side.
- **A LEDGER REASON WITH AN APOSTROPHE** (D414). The emitter wrote
  `'Appa, Aang's Companion'`; a builder quotes by content (`lit()`).
- **A PIN THE REPIN DID NOT LIST** (D414). The D79 `targeted` pin moved with
  the parser (11,944 -> 12,363) and the first repin missed it; run the nine
  pinned suites whole, never the ones a repin names.
- **AN ATTACK-TRIGGER STAPLE IS THIN FUEL, A TWO-SYMBOL ACTIVATOR TOO** (D414).
  Trained Condor read 0 at 60 (the driver rarely attacks), Kiora's Follower
  ({G}{U}) took it to 1; two free sacrifice activators and a colourless Key
  took it to 11.
- **A LATE HEAD WALKS PAST THE PAYMENT PROMPT** (D415). The combat-damage heads
  walked to the postcombat main before stopping at the prompt, and the walk
  answers it (declines); the paid branch was unreachable until the walk was
  removed - the optional-trigger path's rule (D371), one prompt over.
- **THE SELF REWRITE HID THE VOCABULARY'S KEY** (D415). The row maker spells a
  self counter ` on ~.` for its own kinds, but `vocab-ok.json` is keyed by the
  PRINTED sentence; a payOptional whose body puts a counter on this creature
  read as `not a pump` (latent since D400) until the printed form was looked up.
- **A SPLIT LINE SHARES ITS TAG** (D415). `attacks or blocks` is two abilities
  with one tag; a fixture var declared per ability is declared twice - dedupe
  by name per suite.
- **A CAST HEAD'S SPELL IS A PERMANENT** (D415). `castSpiritOrArcane` casts
  Cloud Spirit and was listed permanentless; a board delta under it was one
  short, latent until a self-sacrifice price asserted one.
- **THE TWO NAMED BY THE PORT** (D415). An enters tax on a card with an
  activated line (the harness declines the tax while the activation's board is
  armed - Hecatomb) and the D398 hazard struck by a reshuffled deck
  (High-Society Hunter): refused by name in the row maker's SKIP table.
- **A HEREDOC PART FILE HALVES ITS BACKSLASHES, AGAIN** (D415). A `\n` in a
  template string became a real newline in the part file; the currency mark
  and the char-code composition, every time.
- **THE OXFORD LIST IS ALTERNATIVES** (D416). `an artifact, instant, or sorcery
  card` split on commas and spaces gave the look's reader `artifact instant` -
  one card that is both - and Nimble Larcenist asked nothing; the noun keeps
  its punctuation past the negations and every comma reads as `or`.
- **A SELF-AIMED REVEAL HAS THE CHOOSER FOR AN OWNER** (D416). `Target player`
  can be the caster; an owner branch that required the owner to differ let a
  self-aimed Thoughtseize fall to the plain discard path (no noun, no life).
  The branch takes every prompt with an owner.
- **THE FIRE'S SETTLE ANSWERS THE PROMPT** (D416, D371's rule one prompt over).
  A generated fire ending in `settle(g)` lets the harness pick before the test
  can; a fire whose payload asks stops AT its prompt.
- **AN APOSTROPHE IN A SINGLE-QUOTED MESSAGE** (D416). `'the hand is the
  opponent's'` through the generator's template broke every suite; double
  quotes.
- **A FUZZ STAPLE MUST BE A FIXTURE** (D416). Coercion was named a staple
  before it was pinned; the fixture list is the oracle the L1 theorem resolves
  names in.
- **THE COUNT IS OPTIONAL** (D417). `the top card` prints no number, and a
  rule that required one read none of the singles - the top of the family;
  absent means one.
- **A PERMISSION IS THE PLAYER'S, THE ZONE IS THE OWNER'S** (D417). The
  cast's `from.player !== player` refusal reads the zone's player (the owner)
  and had to admit a permitted exile card; the land's move names the exile
  zone as its `from`, so the funnel sees where it came from.
- **A BACKGROUNDED GATE WITH AN INNER AMPERSAND** (D417). It survived this
  time; the rule stands - launch a gate with `run_in_background`, never with
  a trailing ampersand in a foreground shell.
- **A PERMITTED PLAY IS A FOLLOW-UP PICK** (D417). Twelve permissions and ONE
  play at 60 with one copy of two staples: the random driver rarely lands on
  the permitted card before the deadline; two copies of three staples took it
  to 26 / 2.
- **THE STATE HASH ENUMERATES ITS FIELDS** (D417). `stateHash` lists what it
  hashes, so a new list on the state is silent to the replay check until it
  is added; `playPermissions` is.
- **A REGEX LITERAL AND A `String.raw` TAKE ONE MARKER, A TEMPLATE STRING
  TWO** (D418). A part file written for the heredoc marks every backslash;
  a regex literal or a `String.raw` wants exactly one per backslash and a
  normal template string two - 35 doubled ones had to be halved out of the
  first part, and the parser read every `where X is` form as manual until
  they were.
- **THE PROBE CARD MUST NOT BE NAMED X** (D418). `selfRef` replaces the
  card's own name with `~` before any rule sees the sentence; a parser probe
  named `X` lost every X in its text and read `manual`.
- **A COUNTED SUITE COUNTS WHAT ITS ARM PUT** (D418). The expected count is
  the witness, self when the noun admits it and it stays (a dies head or a
  self-sacrifice cost puts it in the graveyard it may count), the clause
  fixtures and the `no` Cyclops for an any count; a sibling ability that
  puts a permanent (Horn of Gondor's enters token) joins the count the arm
  did not size - refused by name after the port failed on it.
- **THE DOUBLING GUARD** (D418). Krenko doubled his Goblins every untap in
  seed 150 - 255 tokens by intent 95 and the invariant walk quadratic from
  there; a synchronous seed loop is past any vitest timeout, so the first
  gate run's shard 0 spun past its ceiling. The fuzz driver takes no
  activation from a player with forty or more permanents; the stuck shard
  had to be left to die (killing is blocked) and the log archived.
- **A REVEALED HAND IS CLEARED AFTER ITS ANSWER** (D418). The leak gate's
  fixed seed changes shape with every staple; this wave's shape revealed a
  hand, and a standing `revealedTo` on a card that stays in the hand is a
  window the table must not keep. The reveal is cleared over what stays
  (the library rule, D357) and the narration names the cards.
- **A REFIRE IF FIRES ON TURN 7** (D419). D400's arm plays the broken stage
  on turn 5 and the fire on turn 7; a pump-ends-at-cleanup walk to turn 4
  reads a live pump. The walk passes turn 7's cleanup (`endTurnIf`).
- **THE CLASSIFIER MIRRORS THE READER BY HAND** (D419). `BOARD_COND` in
  `primitives.ts` and `parseCond` / `readPred` in `gen-cond.cjs` are two
  spellings of one grammar: a shape the alternation admits that the reader
  refuses lands in the ledger by reason, a shape it refuses that the reader
  would read is a card the pool never sees. Widen both together, never by
  shape.
- **THE HEREDOC HALVES A BACKSLASH-N INSIDE A JS STRING TOO** (D419). An
  applier whose anchor spells a template's newline escape must compose it
  (`BS + 'n'`), or the anchor matches nothing (invariant 14, one form
  over).
- **THE TWO READERS ARE MIRRORED BY HAND, AND THE CLASSIFIER WAS THE
  NARROWER** (D420). The row maker stripped a true ability word since D371;
  the classifier stripped it only under its condition peels, so 57 whole
  cards the generator could row never reached the pool. Measure the seams
  BETWEEN the readers (a line the row maker reads that the classifier
  refuses), not only the readers.
- **THE COPY HALF OF MAGECRAFT IS UNREACHABLE TODAY** (D420). `Whenever you
  cast or copy an instant or sorcery spell` joins the `castInstantSorcery`
  head on its cast half alone: the engine has no spell copy (no
  `SpellCopied` event exists), so nothing the head misses can happen. When
  the copy seam lands, this head must match the copy event too.
- **THE REWRITE IS HEAD-AWARE OR IT IS WRONG** (D421). `recipientAsSelf`
  rewrites a leading `it` unconditionally because the engine only sees
  payloads whose row was built under a self head; the classifier sees every
  line, and an unguarded probe counted 143 whole cards of which half
  (`Whenever a creature you control enters, it gets +2/+0`) were referents
  that would have been scripted as self pumps. The `it` forms are read only
  under a head whose subject is the source.
- **A LEADING `It` IS STORED UNDER THE `~` KEY ALONE** (D421). The vocab-ok
  table is keyed by the printed payload; a printed `It gets ...` that
  answered would let the row maker build a self pump under `whenever
  another creature attacks`.
- **DAMAGE FROM A SOURCE THAT HAS DIED DEALS 0** (D421). `When this creature
  dies, it deals damage equal to its power to any target` - the executor
  reads the source off the board it left; last known information (CR
  113.7a) is an engine gap the port found (Fireblade Charger, Goblin
  Fireleaper), refused by name and ledgered.
- **A BACKSPACE IN A PART FILE** (D421). A heredoc turned a backslash-b
  inside a JS string into a literal backspace character in the written
  probe - invisible in a grep, fatal in a regex. Compose it by char code
  (invariant 14, one form over).
- **THE INGEST AND THE PARSER MUST AGREE ON THE LINE** (D422). `parseEffects`
  scrubs a reminder before it drops `This spell can't be countered.`; the
  ingest's regex read the raw text, and with a strict regex Slice from the
  Shadows (`... (This includes by the ward ability.)`) would have been
  COMPLETE by the parser and COUNTERABLE by the engine - D90's exact
  failure. The ingest admits the trailing parenthetical.
- **`selfRef` RUNS BEFORE THE DROP** (D422). The card's own `This spell` is
  `~` by the time a line filter in `parseEffects` sees it; a filter on the
  printed words alone matches nothing.
- **A REMINDER IS NOT A KEYWORD** (D422). Tier 3's Ward note fired on `(This
  includes by the ward ability.)` and told the player to pay a cost the
  card never prints; the note reads past parentheses now.
- **THE ROW MAKER ROWS NO SPELL** (D422). A spell in the select pool is a
  ledger row by construction; a spell-side seam's yield is the vocabulary's
  alone, measured by the leftover diff (28 seam, 0 rowed).
- **THE PREVIOUS CLAUSE IS THE ANCHOR, NOT THE SENTENCE** (D423). `it deals
  4 damage instead` names no target: `kickedInsteadRewrite` re-asks the
  rules with the base clause's phrase and the new number, so a base the
  rules refused leaves the instead clause refused too (both halves read, or
  neither - D90). `create N of those tokens` refuses a previous token that
  is itself kicked; the count is the sentence's word, never a count
  expression.
- **THE DRIVER ALWAYS KICKED** (D423). The offer carried `kicker` whenever
  the face had one and the driver sent `kicked: 1` every time, so a kicker
  spell was cast only when the kick was affordable and D403's "the next
  pick may cast it plain" was never true (its counters read 1 / 1 at 60).
  The kick is a seeded coin flip now; a rate canary on a kicked branch
  reads the DRIVER's coin, not the pool - the unkicked branch is the
  abundant one and carries the floor.
- **A PROOF TARGET MUST SURVIVE THE PLAIN HALF** (D423). Burst Lightning's
  suite aimed both casts at a 5/2 and the unkicked 2 damage killed it
  before the kicked proof; the suite aims at a 4/4.
- **THE CLASSIFIER IS A HAND MIRROR, AND IT DRIFTS** (D424). Every reader the
  row maker gained since D313 (the optional trigger), D371 (the row kinds,
  the `KW` list) and D374 (the filtered heads) was invisible to the
  classifier until the row maker itself was run over the leftover
  (`make-rows124.cjs -dwide` over every one-piece permanent): 140 rowable
  cards it had never offered. The wide run is the measurement that finds
  the drift; run it before choosing a decision.
- **A STALE LEDGER ROW IS INVISIBLE TO THE GUARD** (D424) until a SEAM
  completes the card: a row the row maker has merely outgrown (a filtered
  head it reads since D374, a payment sentence the vocabulary reads whole
  since D400) stays in the ledger; the wide run names those too (49).
- **THE HARNESS PUT MAY TAKE FROM THE HAND** (D424). `put` finds a named
  card in the library first and in the hand second; a hand-count assert
  measured before a fixture's put is wrong whenever the opening seven dealt
  the fixture - six draw suites under filtered heads read `hand0` for
  `hand0 + 1`. The arm re-measures after a fire whose first statement puts
  (`remeasureHand`); `castsFromHand`'s per-head guesses for the put-based
  heads are gone.
- **A GRANT LIST THE DERIVE DOES NOT READ** (D424). `protection from
  multicolored` / `from creatures` pass the row maker's `PROT` and land no
  protection; the attached static's suite asserts the landwalk and the
  protection now (the helpers were emitted and never read), and caught four.
- **A FIVE-COLOUR BOT DECK CASTS NOTHING** (D424). `buildBotDeck` took the
  widest reach and a five-colour commander with it; cheap spells of every
  colour over basics split five ways cast one to nine spells a game between
  four bots, and the battery's fixed seed drew a game with none. The
  commander is at most two colours now (Emmara, GW); measure a deck by its
  casts, never by its reach.
- **THE SACRIFICED SELF IS `It`** (D425). The vocabulary and the row maker
  spell `This creature` as `~` everywhere and `it` only under a self head;
  an activated line whose cost eats the permanent names the effect's
  subject `It` (`Sacrifice this creature: It deals 1 damage to any target.`
  - Mogg Fanatic), and 49 lines waited on one rewrite (`SELF_SACRIFICE_COST`
  in the classifier, `sacrificesSelf` in the row maker's activated branch).
- **A FIXED NUMBER FROM THE GRAVEYARD IS RIGHT, A POWER IS NOT** (D425).
  `damageTo` derives the sacrificed card where it lies - the printed number
  and the printed keywords are CR 608.2h's answer for `It deals 1 damage`;
  `damage equal to its power` is not (the counters and pumps died with it):
  six suites refused the power form (Cinder Shade, Flame Elemental, Ghitu
  Fire-Eater, Heartfire Immolator, Minotaur Illusionist, Skarrgan
  Skybreaker) and the classifier keeps reading it - the port ledgers it by
  name until last known information is built.
- **A SCOPE WORD IS A SCOPE EVERYWHERE** (D425). `you` in `SCOPE` reaches
  every scoped verb; the player-refusing verbs (destroy, exile, bounce)
  refuse it the way they refuse `each player`, and a pin says so.
- **A FAILED GATE KEEPS RUNNING** (D425). `verify.cjs --full` runs every gate
  to the end even after one fails (the fuzz shards start after a red unit
  suite), so a rerun launched on "gates FAILED" overlaps the first run's
  six shards - two 500-seed gates at once - and the second run's unit suite
  times out (`net.test.ts`, the tournament) for the load, not the tree.
  Wait for the first VERIFY PROCESS to exit (the task notification) before
  launching again; D425 needed three runs for this.
- **A SUPPRESSION PIN IS A GATE'S RED, NOT A BATTERY'S** (D426). A widened
  reader outgrows shipped D187 spell defs, and their `the suppression
  predicate holds` pins go red in the gate (D425 found two there). Run the
  551 suites against the new reader BEFORE the gate - in four batches, the
  command line is too long for one - and retire what they name
  (`retire-426.cjs`: eighteen).
- **A HALF THAT READS AS NOTHING REFUSES THE WHOLE** (D426). The conjunction
  is the closed vocabulary twice, not a wider vocabulary: `Target land.`
  reads as nothing, so `Destroy target creature and target land.` stays
  unread, and an asking left half still lands `assisted` (D195).
- **THE `you` SCOPE CHANGES WHO THE SUITE ASKS** (D426). The generator's
  queued-discard assert read `p2 discarded` for every player scope; four
  `Discard a card.` rows failed until it asked p1 alone (`queueAsksP2`).
- **A SUITE THAT PINS A SPELL'S SILENCE** (D426). Two suites registered one
  script and cast a second card the vocabulary could not read, pinning the
  nothing it did (Student of Ojutai's Succumb to Temptation, Rewards of
  Diversity's Lightning Helix); a wider reader makes the card run, and the
  pin is rewritten, not adapted (D117).
- **THE STALE LIFE** (D426). Two life events for one player in ONE batch
  overwrote each other for as long as the reducer wrote `LifeChanged.to` -
  the second event's `to` came from the snapshot before the first (D295).
  The reducer applies the delta; `effects.ts` keeps a per-resolution ledger
  (`lifeChanged`, `dealt`) so the log's `to` is right too.
- **A COMBAT-ONLY SHIELD IS PROVEN ONLY BY AN ATTACK** (D427). The generator's
  ping is not combat damage: a combat-only shield with a source filter, a
  recipient set or the both-ways form is refused by name like the Fog shape
  (`a combat-only scoped shield the suite cannot prove (it must attack)`),
  or its suite asserts a prevention that rightly did not happen - two rows
  (Cephalid Illusionist, Soratami Cloud Chariot) failed before the refusal
  was written.
- **THE CONTROLLER IS RESOLVED WHEN THE SHIELD IS BUILT** (D427). `you` and
  `your opponents` become `{ player }` / `{ notPlayer }` in the executor's
  `prevent` case; a shield is state and must say who it means, never read
  the caster off the shield later.
- **A REFERENT MID-SENTENCE NEEDS ITS OWN ADMISSION** (D427). The referent
  rewrite reads a sentence that STARTS with the referent or with a verb
  that takes it; `Prevent all damage that would be dealt to it this turn`
  needed `REFERENT_SHIELD` before the rewrite was tried at all.
- **A DAMAGE HEAD IS TWO DEFS** (D428). `dealsDamagePlayer` / `dealsDamageOpponent`
  emit a combat def and an `Any` twin on `DamageDealt` (D371) and a row's
  `playerOf` is copied onto both: one that read only `CombatDamageDealt`
  silenced the twin. The head table reads both damage events.
- **THE FIRE'S OWN COMBAT DAMAGE IS IN THE BASELINE'S WAY** (D428). `p2life0`
  is read before the attack; a referent life loss under a combat-to-player
  head asserts `p2life0 - power - N` (`p2LifeAdjOf`: double strike twice,
  infect none). No earlier row asserted p2's life under a combat head.
- **THE REFERENT PAYLOAD IS THE VOCABULARY'S ALONE** (D428). `parseEffect`
  reads `~ deals N damage to target player` as the `damageTarget` ROW KIND,
  whose emitter asks `ChooseTargets`; a referent row takes `vocabReading`
  of the rewritten sentence and nothing else, or its suite waits for a
  prompt the def never raises.
- **A TOKEN PRINTING SHARES ITS CARD'S NAME** (D429). The first ndjson record
  by name can be the Offspring or eternalized token, whose text lacks the
  keyword line: the lines dump filters `commanderLegality`, and the
  shipped-script DISCLAIM check is what caught the four rows made on the
  wrong text (a def claiming `LINES[0]` for a trigger printed on line 1).
- **THE HARVESTER MUST ASK WHAT THE ROW MAKER ASKS, AGAIN** (D429, D398).
  Every peel before the lookup (`you may`, `Activate only ...`, the bullet
  marker) is a key `collect-payloads` must produce, or the row maker says
  `not a pump` about a sentence the vocabulary reads.
- **THE WIDE RUN IS THE OFFER** (D429). `select.cjs` measures the classifier's
  mirror; the row maker over `poolWIDEALL` is what a wave lands. A wave's
  pool is the wide run's rowed names; the mirror's drift is a reportable.
- **A SHIPPED EXAMPLE CANNOT BE THE UNSHIPPED ONE, AT EVERY SITE** (D429). Ajani's
  Mantra was the disclaim teeth check's example, a TESTING script in the fuzz
  registry (the duplicate-oracleId throw refused gate run 1) and tier3's
  protection example (Kor Firewalker, the same wave): a wave that lands a
  testing-registry card must retire its copy from the fuzz list and move
  every example that leaned on it being unshipped.
- **A COUNTED SHRINK DIES AT THE BASELINE** (D430). A negative per-count pump
  over the hand or the board kills the creature before the suite reads it
  (the graveyard card's printed P/T came back as the baseline); the row
  maker refuses the shape except over the permanent's own counters.
- **THE COUNT'S `who` IS NOT THE ATTACHED SIDE** (D430). Spreading a parsed
  count into a row overwrote the attached pump's `who` with the counted
  controller and the Equipment's row lost its Equip line; the side is
  `host`.
- **A GENERIC READER BEFORE A SPECIFIC ONE REFUSES IT** (D430). The attached
  static's `grantBody` refused `gets +1/+1 for each ...` as a body outside
  the vocabulary before the counted reader was asked; the specific reader
  stands first.
- **TWO ASKS UNDER ONE HEAD ARE TWO QUEUES** (D431). A card whose two entry
  triggers both ask (Marsh Crocodile's return and discard) raises them in
  an order the suite does not control; the row maker refuses the shape by
  name rather than half-asserting one queue.
- **AN OPTIONAL TRIGGER WHOSE PAYLOAD ASKS IS TWO PROMPTS** (D431). The yes /
  no and the queue's pick: the generator's optional path expects a fire
  ending in the settle the asking path strips (Tazeem Raptor); refused.
- **THE FORCED PICK RAISES NO PROMPT** (D431, D390). One land on the board
  returns itself unasked (the queue asks only where there is a choice); a
  suite that waits for the prompt must put two candidates.
- **A VOCABULARY TOKEN NEEDS ITS PRINTING PINNED LIKE A ROW'S** (D431). The port's
  token-pin step read `kind === 'token'` rows only; a conjunction half or a
  referent row that creates a token (Bitterblossom's Faerie Rogue) shipped
  with no fixture printing and the fuzz created it nameless (`tokensNamed`
  short of `tokensCreated`). The row maker pins them at row time now.
- **A PAYER NAMED BY THE HEAD MUST BE ABLE TO PAY, OR NOTHING IS ASKED** (D432,
  CR 119.4). The payment prompt is raised only when the payer can meet the
  price; the unpaid branch runs at once otherwise. A suite that waits for
  the prompt puts the payer's lands first.
- **THE BODY'S `self` HIDES THE PAYER** (D432). An untargeted payment body
  (`you create a Treasure token`) is a self clause, and the aim derivation
  routed the whole `payOptional` to a null aim - `missing`, never asked.
  A target-player payer is resolved off the object's first player target
  BEFORE the self branch.
- **THE HARVESTER CONJUGATES WHAT THE ROW MAKER CONJUGATES** (D432, D398).
  `they pay` harvested as `target player pay` and rowed as `target player
  pays` - two keys for one sentence until `collect-payloads` used the same
  `rewriteThey`.
- **THE TURN'S OWN DRAW IS ALREADY IN THE HAND WHEN THE STEP BEGINS** (D433).
  A `StepBegan` draw head resolves after the turn-based draw; a suite that
  counts the active player's hand from a baseline taken before the draw
  step counts one more than the head drew. The generator's
  `P2_DRAW_CROSSED` carries the offset for every head at or after that step.
- **A SUBJECTLESS RIGHT HALF BELONGS TO THE LEFT'S PLAYER** (D433). `Target
  player draws a card, then discards a card` read the discard as the
  caster's own (a `self` clause) - right after `Draw a card`, wrong after a
  player subject. `conjunctionSplit` re-reads the half with the subject
  continued, aimed where the left aimed.
- **TWO HEADS OF ONE CARD ON ONE STEP ARE REFUSED BY NAME** (D433). Nekusar's
  draw-step head makes the draw its own draw head pings; the card is right
  twice and the one-shot asserts one. A SKIP row, never a read around it.
- **A MILL ROW'S GRAVEYARD COUNT OWES THE FIRE'S OWN DELTA** (D434, D371). The
  generated suite fires an enters row by moving the card OUT of the graveyard
  and a dies row by putting it IN; a sacrifice or discard cost adds its
  fodder. The vocabulary mill takes the self-mill row's one ledger (`gy1Adj`)
  - a second ledger would drift from the first.
- **TWO HEADS OF ONE CARD INTERACT** (D434, D433's class one verb over). A loot
  whose draw fires the card's own draw head raises two prompts where the
  walk answers one (Teferi's Tutelage). Refused by name.
- **A `CardsMoved` WITH NO `reason` IS STILL A MILL** (D434, D355). The hand
  modules and the self-mill rows emit the bare event, so the canary counts
  library-to-graveyard moves; a `reason: 'mill'` waits until every emitter
  goes through `millEvents` (a field only some fill is a dead seam).
- **AN EMPTY LIBRARY STILL ATTEMPTS THE DRAW** (D435, CR 704.5b). `Draw a
  card. If you do, discard a card.` with no library sets the loss flag and
  skips only the discard it gated; a test expecting no `DrewFromEmptyLibrary`
  was the wrong expectation.
- **THE LOOT'S DISCARD READS THE HAND AS DRAWN INTO** (D435). The prompt is
  answered against the post-draw hand; the no-choice branch counts the drawn
  cards in, so a hand of none draws one and discards it whole.
- **AN OPTIONAL TRIGGER WHOSE PAYLOAD ASKS ANSWERS THE YES LAST** (D435, D432).
  The asking rule strips the fire's settle (the prompt is the assert lines'
  to reach) and the optional rule wanted it back; the yes goes last, or before
  the combat walk to the prompt where the fire carries one.
- **THE HARNESS HAS NO TURN 2 FOR P1** (D435). In a two-player harness game
  p1's turns are 1, 3, 5; an `advanceUntil(turn 2 && active p1)` runs to the
  end of the game and the next intent is refused as `gameOver`.
- **A REGEX LITERAL IN A PATCHED `.ts` TAKES ONE MARK PER BACKSLASH** (D435,
  invariant 14). The applier convention (`¤` = one backslash) is applied
  verbatim: `¤¤.` in a regex literal is a literal backslash and matches
  nothing; the pair belongs only inside a template-literal string. The fuzz
  counter read zero over two canary runs before the regex was suspected.
- **THE TARGET DOES THE NARROWING, THE EFFECT ONLY MOVES** (D436, D138). The
  graveyard-card kinds carry no noun, owner or zone of their own: `targetParse`
  enforces `from your graveyard` and `creature card` at the aim, and a rule
  that re-read them would be a second reader to drift.
- **A TEST'S "UNREADABLE" EXAMPLE IS A PIN ON THE VOCABULARY'S EDGE** (D436,
  D117). The primitives suite's optional-residue example was the very sentence
  this seam reads; the example moves to a still-unread one, the rule stays.
- **THE CLIENT AIMS ONLY THE PILE'S TOP** (D436 - a client debt). The zone
  browser renders its cards with `registerSlot={false}`, so the aim veil
  anchors no buried graveyard card: a human aims Raise Dead's kin and these at
  the top card alone; the bot and the engine aim any. A decision of its own.
- **A TRIGGER'S TARGETS CARRY THEIR CLAUSE SLOTS** (D437, D299). A flat
  target list is assigned to clauses by search, never by position; a trigger
  whose optional first clause is left empty shifted the second clause's pick
  into the first executor's aim (the gate's seed 225 - Mechanical Mobster's
  creature exiled "from the graveyard"). The trigger prompt records the
  assignment on `StackTargetsSet.targetSlots` now, and the graveyard-aimed
  executors refuse an aim outside a graveyard.
- **A LIBRARY'S TOP IS READ AFTER THE CLAUSES BEFORE IT** (D437). Two clauses
  of one resolution that both take from a library must see each other's
  moves: `state` is the pre-batch state, `out` what happened so far, and a
  reader that ignores `out` moves the same card twice (the gate's seed 297 -
  `Mill two cards. Draw a card.`). `afterLibraryMoves(state, out)` folds the
  batch for every top reader (draw, mill, look, scry, surveil).
- **`npx tsc --noEmit -p tsconfig.json` CHECKS NOTHING** (D437). The root
  tsconfig is `files: []` with project references, so that command type-checks
  an empty project and prints nothing; `npx tsc -b` is the real check (the
  port scripts run it). Every "tsc green" the loop printed off the root config
  since D433 was vacuous.
- **THE PIN'S PARSE LACKS THE FACE'S GATE** (D437, D187). A suppression pin
  calls `parseEffects(text, name, true)` with no `xCost`, so a hand X def's pin
  holds however whole the vocabulary reads the face under its `{X}`; a
  redundant X def is found by measuring with the gate and retired by name.
- **A FLASHBACK CAST'S X STAGE PRICED THE PRINTED COST** (D437, fixed). The
  cast reads the flashback cost (D307); `chooseX` rebuilt the payment problem
  from `face.manaCost`. It reads the cast's own cost source now.
- **THE RAW FACE'S MANA COST IS A STRING** (D437). `parseFace`'s `face` is the
  `CardData` face (`manaCost: string`); the parsed `ManaCost` is the local
  `manaCost` beside it - the `{X}` gate reads the local, and `tier3.ts` tests
  the raw string.
- **CR 509.1a WAS NEVER CHECKED** (D438). `validateBlockDeclaration` walked
  the declaration per ATTACKER (menace, `cantBlock`, the evasions) and never
  per BLOCKER: one creature could block every attacker, and two engine suites
  (`project.test.ts`, `simplestAnswer.test.ts`) proved the combat-order
  prompts on exactly that board. A rule the fuzz cannot reach (THE DRIVER
  NEVER BLOCKS) stays unchecked until a seam asks the question; a suite that
  relied on the gap is rewritten with a testing def (`blockCapacity: () =>
  2`), never kept (D117).
- **A COUNT HOOK FOLDS, A FLAG DOES NOT** (D438). Three defs may answer for
  one creature (a printed line, a grant, an Aura): `countsBy` takes the
  largest capacity, the largest floor and the smallest ceiling. A boolean
  hook would have made the second def's answer depend on registration order.
- **A DEF ANSWERS `null`, NOT `1`, FOR EVERY OTHER CREATURE** (D438).
  `blockCapacity: () => 2` answers two for the whole battlefield; a generated
  line reads `blocker === self ? 2 : null`, and the scoped forms (`Each
  creature you control ...`) will read their scope in the same place.
- **THE DORMANT `orderAttackers` PROMPT** (D438). A blocker on two attackers
  assigns damage in its DECLARATION order (`attackerOrder`); no producer
  raises the prompt. The first double-blockers shipped this decision make
  the debt real - a decision of its own.
- **THE NET SUITE'S COST GROWS FASTER THAN THE REGISTRY** (D438). The four-seat
  loopback tests: 14-15 s idle at 4,564 scripts, 39-43 s at 6,773; two spilled
  past 60 s under the gate's contention (run 1) while passing alone - growth over
  hang, budget 60 -> 120 s under the ceiling rule - the file default AND the five
  per-test budgets, which override it (run 2 failed on exactly that). The superlinear growth is a
  measurement debt (a four-seat table's per-step work scales with the registry).
- **THE REGISTRY-SCALING WALK** (D438, the mechanism D388's floor owed). Five
  funnels (`staticSourcesFor`, the replacement funnel's `applicableTo` twice,
  `staticPreventions`, the combat walks) compared EVERY registered def of a kind
  against every battlefield permanent by oracle id - 80% of the bot's pipeline
  in the tournament profile. Each asks the source's own script now
  (`scripts.get(oracleId)`): 12.6 -> 79.2 decisions/s, byte-identical canary.
  A walk over `defs` is a walk over the registry; profile before assuming.
- **THE HARNESS DECLINES EVERY PRICE** (D439, D369). `simplestAnswer` answers
  `payMana` with `pay: false` - paying is the one answer that can be rejected -
  so a suite of a permanent with an upkeep price must pay it itself or lose
  the card under test at the first upkeep the walk crosses (an unpayable price
  is not even asked: the permanent goes). The generator's priced suites walk
  through a local `advanceUntil` that pays the card's own prompt, with the
  price's lands staged on p1's side at arming.
- **A PRICE THE ENGINE CANNOT CHARGE IS NOT THE KEYWORD** (D439). `echo` and
  `cumulativeUpkeep` are granted by `readUpkeepPrice` - a mana string, `Pay N
  life.`, or both - and the accounting, the Tier-3 note and the trigger read
  the same reader: one gate, four consumers. `Echo—Discard a card.` is a
  leftover line, not an echo; `Cumulative upkeep {W} or {U}` and `{S}` stay
  unread until a chooser and the snow concept exist.
- **READ THE PRINTING, NOT THE MEMORY** (D439). Shivan Raptor's echo is
  {2}{R}; a test that staged two Mountains for {1}{R} read "the price cannot
  be paid" (the permanent gone, no prompt) - the rule working. Commander
  starts at 40 life: a life assertion written from 20 reads 38 for 18.
- **THE GENERATOR APPLIER'S TWO NEWLINES** (D439, invariant 14). Inside an
  emitted template literal the currency sign spells the `\n` escape; BETWEEN
  emitted lines a real newline is needed - two literal escapes landed in the
  generator's source and broke its load. A SKIP reason with an apostrophe
  wants double quotes.
- **THE ROW MAKER'S `Each opponent loses N life` STOPPED AT THE OPPONENT**
  (D439). A shape that exists for one scope and a vocabulary with none: a
  scope built for one verb (D434's mill) is one rule away for the next.
- **A HEAD THAT READS THE PRE-EVENT STATE MUST CARRY WHAT IT READ** (D440).
  A looks-back trigger matches against the state before its event and
  resolves against the state after - a dead creature's counters are gone by
  then. `KeywordTrigger.memo` is one number read at collection and carried
  onto the stack object (`PendingTrigger.memo` -> `StackObject.memo`); the
  label and the item were the only other channels, and a number belongs in
  neither.
- **A NATIVE SYNTHESIZED ABILITY NEEDS NO DEF, AND THE GRAVEYARD OFFER ASKED
  FOR ONE** (D440). `legal.ts` gated every graveyard activation on
  `activatedDefRegistered`; scavenge's counters are the loop's own, so the
  gate takes `scavenge === undefined &&` - the second synthesized ability
  offered from a zone other than the battlefield, after cycling.
- **THE PRINTED BASE IS NOT THE BASELINE OF A CREATURE THAT ENTERS WITH
  COUNTERS** (D440). The generated suites assert counters and P/T against
  the printed base; an entry-counter keyword (modular; graft, devour and
  fabricate would be the same) shifts both, and the row maker refuses the
  pair until the generator adds the entry counters to its baseline.
- **CR 514.1 IS NOT IMPLEMENTED** (D440). The cleanup step never discards to
  hand size, so `You have no maximum hand size` cannot be claimed - the line
  exempts a player from a rule the engine does not enforce. A rule decision
  of its own (every bot game, the fuzz's hands, the tournament's balance).
- **THE ACTIVATED LABEL IS THE EFFECT TEXT** (D440). A stack object put by an
  activation is labelled `${name} — ${effectText}`; a fuzz counter keyed on
  the keyword's name reads zero - key it on the rule's sentence.
- **A PROMPT NEVER CARRIES THE HAND** (D441, D137, D415). A reveal price's
  candidates are hand cards and the `AwaitingSet` is projected to every seat:
  the prompt carries the NOUN (`entersChoice.reveal`), the answerer reads its
  own hand, the host checks the card named (`revealAdmits`). A list of ids on
  the prompt would tell every seat which cards match.
- **AN ASKED CONDITION IS A COMPILE-TIME SET** (D441, D136). `isAskedCondition`
  and the two `Exclude<EntersTappedCondition, { kind: 'payLife' | 'reveal' }>`
  types (`conditionHolds`, the activation `board` condition) keep a question
  out of the board queries; a new asked kind goes into all three or `tsc -b`
  says so. D441 is the first widening of D136's instrument.
- **ONE COPY OF A STAPLE THAT NEEDS A SECOND CARD IN HAND IS NOT FUEL** (D441,
  D398). Port Town asks only with a Plains or Island in hand at the drop - one
  of each in ~150 cards; one copy read 0 asks at 60 seeds, three read 2. A
  staple whose condition is a coincidence of two cards wants the multiplicity.
- **A SPLIT HEAD'S ETB HALF FIRES AS THE DIES ARM STAGES THE CARD** (D441). The
  generated dies arm puts the card down and kills it; with the etb half on the
  same payload an ABSOLUTE assertion (a counter on the fixture) reads two for
  one, a delta assertion from the arm's baseline reads right. The row maker
  refuses the absolute case by name (Hunting Moa) until the generator counts
  the etb half.
- **A TURN-BASED ACTION THAT ASKS STOPS THE LOOP WITHOUT FINISHING THE
  STEP** (D442). The cleanup case returns the discard prompt and NOT
  `TurnBasedActionsDone`; the answer re-enters the case, re-reads the hand
  and only then runs CR 514.2. The repeated cleanup (CR 514.3a) relies on
  `StepBegan` resetting the marker so the step asks again.
- **A TRIGGER DURING CLEANUP MUST SET THE REPEAT** (D442). `grantsPriority`
  reads `cleanupNeedsRepeat` and `priority()` ends the step without
  looking at the stack; the SBA arm set the flag from the start, the
  trigger arm is D442's - the discard made a cleanup trigger reachable.
- **THE GENERATED SUITES DO NOT MODEL THE DISCARD, AND SAY SO** (D442).
  `options: { maxHandSize: null }` in every arming: a count taken across
  a cleanup with eight in hand is off by the discard otherwise. A rules
  test that walks turns counts draws off the LIBRARY (monarch, delayed).
- **THE HARNESS DISCARDS THE FIRST CARDS OF THE HAND** (D442). A staged
  card is appended and survives one cleanup; `findAnywhere` finds a
  discarded one in the graveyard, so `put` never fails - a count does.
- **THE ISOLATED CORPUS RUN IS 51 MINUTES** (D442). `vitest run
  src/engine/scripts/cards` pays the oracle import per file; the gate's
  `--no-isolate` is the fast path. Measure a rule against the corpus
  with it.
- **THE BATTERY READS THE ANIMATED VIEW, WHICH LAGS THE ENGINE** (D442). Its
  walkers answer the cleanup discard off the seat's own hand through its
  view; the may driver reads the prompt off the engine snapshot, never the
  view whose `awaiting` never catches up on the last group.
- **EXERTABILITY NEEDS THE PRINTED PERMISSION AND THE SCRIPT** (D443). A
  registry-only reader lets a `Whenever you exert a creature` watcher
  without the line be exerted (CR 701.39a forbids); a face-only reader
  lets a scriptless card pay the untap for nothing (D90). `canExert` asks
  the face, the script and the abilities; the handler asks it again.
- **AN OLDER PRINTING NAMES ITSELF AND KEEPS ITS REMINDER** (D443). `You
  may exert Trueheart Twins as it attacks. (An exerted creature ...)` -
  the dump's strip and selfRef hid both from the row maker; a reader of
  a printed sentence takes the short name and drops the reminder, and the
  accounting asks the reader, never a constant against a scrubbed line.
- **A TARGETED FIRE MUST END IN SETTLE** (D443). The generator's guard;
  an arm's own assertion goes before its settle, not after.
- **A STAPLE TABLE CHANGE RESHAPES EVERY POOL** (D443, D180). Two exert
  staples moved the rotation and D403's kicked-entry floor read 0 over
  500 seeds; the next reshuffle read the exert floor at 0 over 60. The
  third rot's mechanism: the offer's `kickerAffordable`, a kick taken
  exactly when payable and over any other pick, a three-mana kicked
  permanent beside the four-mana one; the exert staples are two-drops
  and the driver exerts every exertable attacker. Read every marginal
  canary at 60 seeds after ANY staple change, not only the new one.
- **AN ENTRY CHOICE IS NOT A PRICE** (D444, D136). On `entersChoice` with
  an `option`, `pay` is the +1/+1 counter and the decline never taps; the
  handler branches on the option before it looks at the price.
- **THE BLOCK RULE READS THE COUNTER, NOT THE CHOICE** (D444). Unleash is
  `can't block as long as it has a +1/+1 counter on it`: a counter put
  later counts, one removed frees it. `riotHaste` is the one remembered
  choice, and it is cleared with the battlefield fields on leaving.
- **READ THE MANA COST BEFORE NAMING A STAPLE** (D444). Arcbound Slasher
  was chosen as a colourless riot Cat and is {4}{R}; the probe, not the
  canary, said why the leg read 0.
- **THE GRANT IS THE LINES BELOW, NOT THE CARD'S KEYWORDS** (D445). Backup
  grants the abilities printed below it; Scryfall's `keywords[]` is
  unordered, so the reader walks the text. A Flash above the line is not
  granted; a static or an activated ability below it refuses the whole
  keyword - half a backup is D90's half-execution.
- **A REPORTED COUNTER SAYS WHAT IT COUNTS** (D445). `backupGrants` read
  ten against five fires: it counted every keyword-only until-end-of-turn
  grant in the log. Named `keywordGrants`, labelled "all sources".
- **THE DRIVER'S ECONOMY IS THE CEILING ON EVERY COLOURED CANARY** (D445,
  D180). The pool dealt eight percent land and the uniform pick skipped
  land drops; a {2}{R} sorcery sat in hand unaffordable at every probe.
  The driver takes the land drop first, deals ten basics of each colour,
  and aims at random; every rate moved - read all floors at 150 seeds
  after a shape change, and probe the driver before multiplying a staple.
- **A TOKEN INSIDE A PAY BRANCH IS NOT A tokenRef** (D445). The port's
  token-pin step pins the refs a module names; the vocabulary resolves
  its own out of a payload string, pay branches included, and one sat
  unpinned for two hundred decisions until the fuzz could pay the {2}.
  `tokenPins.node.test.ts` reads every module the way the fuzz would.
- **A CONDITION THE ARMED BOARD MEETS IS BROKEN FIRST** (D446, D398). The
  `yourTurn` condition holds on turn 3; the `baselineMet` stage walks to
  the opponent's turn 4, reads the static absent, then walks to turn 5
  and reads it present. A suite that only met it would prove nothing.
- **A FLAVOR WORD IS NOT AN ABILITY WORD** (D446). `TRUE_ABILITY_WORD`
  holds the rules' words; Universes Beyond prints arbitrary ones before
  a dash (`Jump —`, `Leap Strike —`, `Bear Form —`). Read past
  `<Capitalized words> — ` for the one shape that needs it, never in
  general - `Choose one —` and `Threshold —` mean something.
- **A COST THAT NAMES NO KIND IS STILL DETERMINISTIC WHEN THE KINDS CANNOT
  COEXIST** (D447, D319). `Remove a counter from this creature` was refused
  because the engine "cannot enumerate what it cannot represent"; the rules
  answer differently - +1/+1 and -1/-1 annihilate in pairs (CR 704.5q), so a
  permanent carries one kind at priority and `kind: null` takes it. Read what
  the engine holds AND what the rules let coexist before calling a price a
  decision. The chooser over the board still names its kind.
- **THE ENTRY COUNTERS ARE THE BASELINE, NOT A SEED** (D447). A suite that
  loads `count` more -1/-1 counters onto a creature that entered with them
  kills it before the activation; `entryCovers` reads the row's own
  unconditional entry counters and seeds nothing when they pay.
- **A KEYWORD'S YIELD IS THE REST OF THE CARD** (D448). Strip the keyword line
  from every leftover permanent that prints it and row what remains
  (`measure-kw.cjs`); a keyword's card count says nothing. Unearth: 55
  cards, 29 landed. Station: 31 cards, 0.
- **THE DELAYED TRIGGER TOLERATES ITS SOURCE'S ABSENCE** (D448, D402).
  `exileSelf` exiles the source only from the battlefield; the leave
  replacement may have exiled it first, and the end step then finds nothing
  - the rule (CR 702.84c), not a gap. A delayed self-aimed effect armed by
  the engine itself is fine; one READ off a card's text still is not (D402).
- **A KEYWORD PRINTED AS A COST IS THE COST GRAMMAR'S, NOT THE KEYWORD
  TABLE'S** (D449). Evoke and dash are elected at cast; the keyword table
  reads the MARK the cast left (`evoked`), never the keyword alone - a
  Mulldrifter cast for {4}{U} carries the keyword and owes no sacrifice.
- **THE ENTRY MOVE CARRIES THE ELECTION** (D449, D403). `alternativePaid` is
  the stack object's; the permanent learns it through `CardMove.altKeyword`
  after the entry reset - a mark set any earlier is wiped by
  `clearBattlefieldFields`.
- **A TABLE ENTRY IS NOT AN ACCOUNTING** (D450, D338). The keyword table
  ran vanishing and fading before `engineComplete` counted them; the first
  wide run rowed zero and the post-seam leftover probe did not move. A
  numbered keyword the table runs joins `NUMBERED_TRIGGER_KEYWORDS`; the
  probe after a seam is the check, and it said so.
- **THE SUITE'S WALK IS AN UPKEEP OR TWO** (D450). A card put on turn 1 and
  fired on turn 3 (or 5 for a late head) has been through its controller's
  upkeep once (or twice); a counted-down keyword with fewer counters is dead
  before the fire. The row maker refuses by count, by name.
- **A FIXTURE'S COMBAT IS THE FIRE'S COMBAT** (D451). The attacking Bears
  declared in `preFire` serve an activation that fires in that same combat;
  a trigger head that walks to its own declaration finds the fixture no
  longer attacking (`illegalTarget`, or a walk past the game's end). The
  refusal is by head, not by name.
- **A ROW'S FIRST PORT MAY BE PULLED, AND THE PULL IS BY HAND** (D451, D450).
  `land.cjs` registers every generated module; a row refused after the port
  owes the registry's import and entry, the WANTED line, the two files and
  the fixture title (`repair-451.cjs`). The stale-entry guard then says which
  ledger rows the wave retired - delete them, the guard's own instruction.
- **THE FIXTURE JOINS THE HEAD'S DECLARATION, NEVER ITS OWN** (D452, D451).
  An attacking fixture under a trigger head is declared in the head's own
  `DeclareAttackers` beside self; a separate declaration in `preFire` fires
  the head early or walks past the game's end. The head list is closed and
  the replacement throws when the arm's declaration is not the plain one.
- **A PRICE TAKEN FROM THE HAND MOVES EVERY HAND ASSERT** (D452). A
  discard-self cost is a card gone before the payload is read: a Channel
  draw is `hand0 + 1 - 1`, a graveyard return the same.
- **A LAYER-2 EFFECT IN A STATE ENGINE IS TAKEN AND GIVEN BACK, AND THE
  SWEEP IS WHERE** (D453, D393). The controller is a field, not a
  derivation; the moments it must move are the moments an Aura's attachment
  changes, and every one ends in the state-based sweep. Put the rule there
  once (`controlledVia`), never at the cast, the attach and each leave.
- **THE MEMORY CARRIES THE WAY BACK** (D453). `revertTo` is whoever
  controlled the permanent when the Aura took it - not the owner; a returned
  Aura is a new object that holds nothing (the entry stamp).
- **THE LEFTOVER'S FRAGMENT IS A WHOLE LINE'S ADDRESS** (D454).
  `engineComplete` blanks a quoted body before it compares lines, so a
  grant is reported as `Enchanted creature has` at line −1. The row maker
  re-joins it by prefix among the printed lines; a row that read the
  fragment as its line would claim a line the module never prints.
- **THE GRANTED ROW'S `~` IS THE HOST** (D454, D367). The recipient is the
  ability's source (CR 113.7a): the Bears taps, pays, is pumped and untapped,
  and the Aura is cast out of the hand in the same fire (the hand one fewer,
  the board one more). The module's readers want the PRINTED body ("This
  creature"); the vocabulary's `~` is its own spelling.
- **THE HOST IS A PARAMETER, NOT A NAME** (D455). The suite's host variable
  stays `bearsB` - every attached assert reads it - and only the card put
  there changes (a Forest for an Enchant land Aura). A land host has no P/T
  and is never summoning sick, so nothing else in the suite moves.
- **A QUOTED BODY THAT NAMES THE PROVIDER IS TWO OBJECTS** (D455, D454). The
  printed name inside the quote is the Aura or Equipment (Hankyu's aim
  counter goes on Hankyu); the vocabulary's `~` is the ability's source - the
  host (CR 113.7a). Refuse before the name is rewritten, or the row reads
  the two as one.
- **A GRANTED TRIGGER IS THE HOST'S LINE, AND ONLY ITS ID SAYS SO** (D456,
  D368). The def is the provider's, read at the bus with the recipient as
  `self`; nothing in its `matches` or `resolve` knows it is granted. The
  `gt` marker on the abilityId is the whole seam - drop it and the def is
  indexed under the provider alone, where the host's walk never finds it.
- **THE ARM CASTS, THE FIRE FIRES** (D456, D454). A granted ACTIVATION casts
  the Aura in its fire (the offer is on the host only once attached); a
  granted TRIGGER has the Aura cast in the arm. The two rows differ in where
  the attach lives, and the hand / board baselines move only for the first.
- **THE REDUCER HOLDS NO ORACLE, SO THE STACK OBJECT CARRIES THE FACT**
  (D457). The exhaust memory is stamped as the ability goes on the stack,
  and the only way the reducer knows the ability is exhaust is the flag the
  handler put on the object. A refusal that read the oracle in the reducer
  would be a second parse.
- **A KEYWORD IN AN ABILITY WORD'S SHAPE IS NOT STRIPPED, IT IS READ** (D457,
  D342). The ability-word list stays closed; Exhaust joins the parser as its
  own reader with its own flag, so the cost is charged AND the rule kept.
  Boast and Channel keep waiting the same way.
- **A TURN RECORD THAT COUNTS IS NOT ONE THAT NAMES** (D458, D348). Raid's
  `attackers` counted the declaration; Boast asks WHICH creature attacked,
  so the record carries the ids beside the count - one reducer site, one
  event, both facts.
- **THE POOL EMPTIES ON THE WAY INTO COMBAT** (D458, CR 500.4). A boast is
  paid where it is activated - after the declaration, in combat. The suite's
  mana lines ride the fire, past the recipe that attacks, never the main
  phase; the engine test learned it the hard way.
- **A KEYWORD TRIGGER MAY ASK, AND THE TABLE SAYS SO** (D459, D343). The
  modes are declared on the table entry and ride the pending trigger like a
  def's; nothing downstream knows the trigger came from the table, and the
  resolution reads the chosen mode and nothing else.
- **THE HARNESS PICKS THE FIRST MODE, SO A GENERATED SUITE READS THE
  FABRICATED BASE** (D459). A `Fabricate N` card enters with N counters in
  every suite the generator writes (the harness answers the prompt with mode
  0); its base is the printed one plus N, or the self asserts read a creature
  that does not exist.
- **A COST THE CLIENT PREVIEWS IS READ OFF THE VIEW, NEVER OFF A PRINTING
  IT CANNOT SEE** (D460, D53). The ward of a face-down disguised permanent is
  public information the projection carries (`CardView.disguised`); the host
  and the client price it off ONE constant (`DISGUISE_WARD`). A host charge
  the client never showed is D53's exact failure.
- **A FACE-DOWN PERMANENT HAS NO PRINTED WARD** (D460, CR 708.2). The host's
  tax read the real face of a face-down permanent, so a morph that prints
  ward would have charged an opponent a ward it does not have. The face-down
  branch charges the disguise ward or nothing.
- **A COMPOUND HEAD IS TWO DEFS ON ONE LINE, AND THE ACCOUNTING MUST SEE
  ONE** (D461, D380). Both halves ride the same printed index; a splitter
  that gave the second half its own line would claim a line the card never
  prints.
- **THE FACE-DOWN ENTRY IS NOT AN ENTRY THE TRIGGER SEES** (D461, CR 708.2).
  The enters half of the compound fires on a face-up entry only, because a
  face-down permanent has no abilities - the bus's `hasAbilities` gate is
  the whole rule, and nothing was added for it.
- **A CHOOSER PREDICATE MAY READ THE COMBAT, AND IT READS THE DECLARATION**
  (D462, D352). `unblockedAttacker` is answered off `combat.attackers` and
  the sticky `becameBlocked` (CR 509.1h) - never off a creature's
  "attacking" look - in the one function the offer and the host both ask
  (D168's shape).
- **AN ENTRY INTO COMBAT IS AN EVENT, NOT A FLAG** (D462). The ninja joins
  `combat.attackers` by `AttackerAdded` and the returned creature leaves by
  `RemovedFromCombat` in the cost batch; the combat state never names a
  card in a hand, and the damage step reads the list it always read.
- **A TOKEN THAT CEASES TO EXIST IS NOT IN THE FINAL STATE** (D463, CR
  704.5d). A counter that reads `state.cards[id]` for a token the end step
  sacrificed counts nothing; the log is the record. The first read said 4
  mobilizes and 0 Warriors.
- **A SEAT HOLDS THREE BASICS, SO A FOUR-DROP IS NEVER A STAPLE** (D463,
  D445). Dragonback Lancer read 0 / 0 over 150 seeds for the driver's
  reason, not the seam's; the two-drop Shock Brigade reads. Pick the
  cheapest printing of a keyword for the fuzz.
- **A FIXTURE NAME IS A DECK LISTING, AND TWO ROLES NEED TWO LISTINGS**
  (D464, D374, D380). The same card name for a cost fodder and a filtered
  head's entrant (or an anthem's witness) makes the second `put` find the
  first copy on the battlefield - no entry at all. Every collision top-up
  lives outside the deck's Set; this compound added two.
- **THE OTHER HALF'S ARM KILLS THE OPPONENT'S CREATURE, AND THE OPPONENT'S
  GRAVEYARD GROWS FIRST** (D464). A mill aimed at p2 under
  `anyOtherCreatureDies` reads the Cyclops in the graveyard before its own
  card; the baseline owes the +1 (`p2gyAdj`).
- **A TERNARY OVER A PROMPT KIND HIDES BOTH PRODUCERS** (D465, D357). The
  awaiting pin scans `kind: '<name>'` constructions, so an entry site that
  computes the kind (`face.choosesColorOnEntry ? 'chooseColor' :
  'chooseCreatureType'`) loses BOTH producers; write one object literal per
  kind.
- **THE WALK ANSWERS THE ENTRY PROMPT, `settle` DOES NOT** (D465).
  `advanceUntil` checks `done` before answering, so a `settle` (stack empty,
  no pending triggers) after a move that raised a prompt returns with the
  prompt standing. The generated arm enters the card before the turn-3 walk
  whose predicate says `awaiting === null`; the harness's Bear is what the
  statics then read.
- **A FUZZ RATE READ AT 60 AND 150 IS THE DEAL, NOT THE MECHANISM** (D465,
  D463). A two-a-seat staple's floor read 0 at both sizes on a retuned pool
  (15 over the previous gate's 500) while a twelve-a-seat probe fired eight
  times in twenty seeds; raise the density before the gate rather than lower
  the floor.
- **A ROW THAT WAS ALWAYS REFUSED FIRST ON ONE LINE HAS NEVER MET THE
  GENERATOR ON ITS OTHERS** (D466). Widening a reader lets a card's second
  lines reach the generator for the first time; a SELF damage head on a
  creature with no printed power (Surge Mare, a 0/5) walked the game out
  waiting for a prompt its attack could never raise. The guard is the row
  maker's (`a self damage head on a creature with no printed power`), not
  the suite's.
- **THE REFUSED BLOCK IS THE PROOF, AND IT NEEDS A BLOCKER THE PREDICATE
  NAMES** (D466, D438). `by X` is proven on an X the suite deals to the
  opponent (the `blocker` fixture base: defender allowed, power may be 0),
  `except by X` on the Cyclops outside X; a row whose exception the Cyclops
  satisfies is refused rather than proven by a block refused anyway. The
  fuzz driver never blocks, so `canBlock` is proven by the unit suites alone.
- **A BATCHED HEAD AND A SINGULAR HEAD ARE TWO DEFS OVER ONE EVENT** (D467,
  D185). `one or more creatures you control deal combat damage` fires once
  per damage step - the matcher alone; `a creature you control deals combat
  damage` once per connecting source - `perItem` over the same conjuncts,
  deduplicated in event order. A suite with one attacker cannot tell them
  apart, so the wording decides and the def says which; never read the
  count a suite happens to stage as the rule.
- **THE COMBAT FIXTURE IS A STILL FIXTURE THAT MOVES** (D467, D379). It
  waits on the board from the arm like a tap or untap fixture, but its fire
  is an attack that costs the opponent its power: the p2 life baseline reads
  `filterFixturePower` off the row, not the Bears' 2.
- **A COUNTER KIND IS A RULE BEFORE IT IS A NAME** (D468, D90). Shield (CR
  122.1i), stun (122.1j), the keyword counters (122.1c), finality, loyalty,
  defense, level, lore, time, fade, age and depletion do something by the
  rules the moment they sit on a permanent, and the engine reads none of
  them off `counters` (`derive` reads +1/+1 and -1/-1 alone). A generic entry
  replacement refuses them by name (`RULES_COUNTERS` in the row maker):
  `counters['shield']` in the state is not a shield.
- **A NAMED ENTRY COUNTER UNDER A COUNTED STATIC MOVES THE BASE THROUGH THE
  STATIC** (D468, D430). Trawler Drake enters with an oil counter and gets
  +1/+1 for each; the entry assert leaves the P/T to the counted static's
  own suite, or the two contradict each other on one board.
- **ONE DAMAGE EVENT, ONE COUNTER, HOWEVER MANY ENTRIES** (D469, CR 122.1i).
  The prevention funnel spends a shield counter per damage BODY and carries
  the count across the batch (`shieldsLeft`); a per-entry spend would strip
  two counters from a double block, and a per-batch one would let a first
  strike and a regular damage step share a counter.
- **THE SBA REPLACEMENT LEAVES THE DAMAGE MARKED** (D469). A shield counter
  replaces the destruction, not the lethal damage that caused it; the next
  state-based pass destroys the creature unless another counter stands. That
  is the rule (a counter put after the damage is spent for nothing), not a
  loop to fix.
- **A VOCABULARY KIND IS ADMITTED THE DAY THE ENGINE READS IT** (D469, D130,
  D468). `CounterKind` was closed at +1/+1 and -1/-1 because those were the
  two the engine applied; the shield counter joined the day the funnel and
  the destroy sites read it. The row maker's `RULES_COUNTERS` is the same
  list from the other side - a kind leaves it only when its rule is built.
- **`on it.` AFTER A TARGET IS THE TARGET, NOT THE SOURCE** (D470, D373,
  D392). The trigger bridge (`recipientAsSelf`) spells a trailing `on it.`
  as `~` only when no `target` phrase precedes it in the payload; after one,
  `it` is that target and the vocabulary's counter referent
  (`REFERENT_COUNTER`) aims it. The blanket rewrite put the stun counter on
  the source, and only a suite reading the counter on the right permanent
  caught it.
- **A FUZZ STAPLE MUST BE A FIXTURE THE WAVE SHIPPED** (D470). The staples
  table resolves every name in the fixture oracle at the start of the run;
  a card with the right text but a second unread line (Deluge Virtuoso) is
  no fixture and fails every shard at once.
- **A BLOCK PROOF BESIDE A TAP OF THE OPPONENT'S BLOCKER WALKS THE GAME
  OUT** (D470, D232). The engine skips the declare-blockers prompt with no
  legal blocker; a suite whose entry taps and stuns the Cyclops and then
  waits for that prompt runs 20,000 steps into a finished game. The row
  maker refuses the pair by name.
- **A KEYWORD COUNTER IS AN ABILITY, NOT A KEYWORD PRINTED** (D471, CR
  122.1c). `derive` adds it at layer 6 beside the temporary grants, so it
  goes with the rest under a lose-all-abilities effect; reading it in layer
  1 beside the printed keywords would keep a flying counter flying on a
  creature Humility has stripped. A kind the engine does not enforce
  (exalted, decayed) stays inert and refused by name.
- **THE CARD'S OWN CYCLE FIRES FROM THE HAND, AND THE HEAD LOOKS BACK**
  (D471, D306, D377). The cycling cost has moved the card to the graveyard
  before the bus asks; `activeZones: ['hand']` with `looksBack: true` is the
  whole trick. The row starts in hand like a spell it casts, the fire pays
  the printed cycling cost and activates `cyclingIndex(g, self)`, and the
  hand baseline needs nothing: the card leaves and the cycling draws.
- **THE LOYALTY COST IS THE COUNTERS, CHARGED AS THE ABILITY GOES ON THE
  STACK** (D472, CR 606). `handlers.ts` emits a `CountersChanged` of
  `loyaltyCost` on the source in the cost batch, before the stack object;
  a plus at zero counters is legal and a minus is offered and accepted only
  with the counters to pay it (606.5). The SBA (704.5i) then sees the
  counters after the cost - a walker at exactly N under a `-N` goes to the
  graveyard with the ability still on the stack (untested by the wave).
- **ONCE A TURN IS PER PERMANENT, NOT PER ABILITY** (D472, CR 606.3). The
  reducer's `recordActivation` counts `${source}|loyalty` off the stack
  object's `loyalty` beside the ability's own key; `legal.ts` and
  `handlers.ts` read the permanent's key, so a `+2` bars the `-1` the same
  turn. The sorcery timing rides `sorceryOnly`, set by rule for a loyalty
  cost.
- **A PAYABLE LOYALTY ABILITY WITH NO DEF IS CHARGED FOR NOTHING** (D472,
  the Krenko precedent of D122). Every un-rowed walker now carries one
  charged-for-nothing note per ability (tier3's `fourOrMore` 24 -> 99);
  a row completes a walker only when EVERY loyalty line reads, so the 291
  left are the emblems (65), the ultimates and the loyalty statics.
- **THE QUOTE IS THE CARD'S TEXT, SO IT MUST RUN** (D473, D90). A token
  description that quotes the printing's rules text is baked into
  `TOKEN_TABLE` only when `engineCompleteness(printing)` holds - the quoted
  ability runs on the token by parse (a mana ability today: the Eldrazi
  Scion and Spawn, the Elf Druid, the Human Monk, the red Wizard). Fifty
  quoted printings resolve and stay refused: a Pest whose dies trigger
  nothing fires, a Rat whose can't-block nothing enforces, a 0/0 Construct
  whose pump nothing derives - Digsite Engineer's died on arrival and the
  suite read the board one short. The predefined artifacts (Treasure, Food,
  Clue, Blood, Map) keep D174's precedent: the card prints no quote.
- **FOLD BEFORE THE SCRUB AND BEFORE THE SELF REWRITE** (D473). `selfRef`
  (effectParse) and `recipientAsSelf` (the vocabulary) both rewrite `this
  creature`, and a quoted mana ability contains it: `foldTokenQuotes` lifts
  the quote out of `priced` first, the mark `#qN#` survives `scrub` (no
  quotes, no parentheses), and `recipientAsSelf` rewrites the spans outside
  the quotes only, the anchored forms on the opening span.
- **THE PRINTING SAYS `this creature`, THE CARD `this token`** (D473, CR
  111.4). Scryfall's token oracle keeps the older self-reference; the
  comparison in `matchToken` reads `this token|creature|permanent|artifact|
  enchantment|land` as one word and the key keeps the card's wording
  verbatim (two wordings, two keys, one printing).
- **A MARK IS NUMBERED PER FACE** (D473). The bake carries each face's
  quotes beside the clause (`JSON.stringify([clause, quotes])`), so two
  faces' `#q0#` never meet in one key; `tokenPrintingIdsIn` and
  `tokenNamesIn` fold per face the same way.
- **THE LINES PROBE READS THE LIVE TREE** (D473, the loop). A row already
  ported reads as complete (no leftover lines, "no ability parsed"), so a
  re-run of the wave chain after a port must reuse the pre-port dump - the
  wide run's `rows-oneshot-dwideall.cjs` served as the wave's rows here.
- **A TOKEN'S DIES TRIGGER AIMS BY LAST KNOWN INFORMATION** (D474, CR
  603.10 / 704.5d). `TokensCeased` deletes the instance before the trigger
  is put on the stack, and `targetingSourceFor` returned null - CR 603.3d
  then removed the Devil's ping as a trigger with no legal target. A TOKEN's
  pending trigger carries `lki` (its printing and face), the `chooseTargets`
  prompt carries it on, and the aim, the answer handler, the fuzz driver and
  the harness read the face from it when `state.cards[source]` is gone. A
  card's trigger carries no field (the same hash).
- **A TOKEN IS A SCRIPT, NEVER DEALT** (D474). The fuzz derives its rotating
  pool from `SHIPPED_SCRIPTS` minus the oracles whose layout is `token`: a
  token is not a library card, and a name shared with a card (`Wizard`)
  would deal the wrong thing. The L1 theorem covers the cards alone.
- **THE MAKER'S SUITE REGISTERS ITS TOKENS' SCRIPTS** (D474). A 0/0
  Construct under a suite that knows only the maker dies on arrival and the
  board reads one short - D473's first, ungated port said so. The generator
  reads the tokens a row creates (the vocab effects, a paid payload's
  branches, a table key) against `token-scripts474.json` and imports them
  beside the row's own export.
- **`this token` IS `this creature` FOR PARSING, VERBATIM FOR THE CLAIM**
  (D474). The row maker and the harvester normalise the lines dump and the
  parse text; `printed` and the def's `text` are the printing's own line.
  The token whose NAME is in its own token description (the Ooze making
  Oozes) reads `~` there and is refused - the self-reference rewrite is
  the trap.
- **A TOKEN ROW IS CREATED, LATE FOR A STEP HEAD, AND ABSENT WHEN IT
  LEAVES** (D474). `ManualCreateToken` by the pin's printing id where a
  card is put; after the walk to turn 3 for a draw or upkeep head (the
  Spirit's counter read turn 3's draw); `toBeUndefined()` where a card
  reads `graveyard` (CR 704.5d).
- **A CAST-INSTANT FIRE ADDS NO PERMANENT** (D474). The Ritual the
  cast-instant and cast-noncreature fires cast resolves and goes; the board
  assert reads the payload alone.
- **AN EMBLEM'S ABILITIES ARE READ FROM THE COMMAND ZONE** (D475, CR
  114.1). `staticSourcesFor` walks each player's command zone after the
  battlefield and takes only a def declaring `command` among its active
  zones - an emblem row's module declares `command` everywhere, a
  commander's script declares none; the trigger bus honoured
  `activeZones` per instance already. The emblem's timestamp (CR 613.7c)
  is approximated by the walk order: after every permanent.
- **THE EMBLEM'S FIXTURE IS BY NAME, ITS SUITE'S SELF BY INTENT** (D475).
  An emblem record's name is its walker's plus `Emblem` (unique, layout
  `other`), so `WANTED` pins it like a card; the suite gives it by
  `ManualCreateEmblem` and reads `zones.command.p1`. A token's is by set
  and number, by `ManualCreateToken`, on the battlefield (D474).
- **THE CHOSEN TYPE IS THE WALKER'S, NOT THE EMBLEM'S** (D475, CR 114.3).
  Oko, Shadowmoor Scion's emblem (`Creatures you control of the chosen
  type get +3/+3 ...`) would read the emblem instance's own `chosenType`,
  which is null - refused by name in the row maker's SKIP table.
- **AN ALTERNATIVE'S TYPES ARE ALL-OF, THE CLAUSE'S ANY-OF** (D475, D297).
  A printed list whose alternatives differ is aimed at the first
  alternative some fixture satisfies, and the fixture is checked against
  EVERY type of that alternative (`artifact creature` is both) before it
  serves.
- **A ONCE-A-TURN PROBE CANNOT RUN UNDER THE PAYLOAD'S OWN PROMPT** (D475).
  A loyalty ability whose payload ASKS (Vivien Reid's look) has its prompt
  up when the fire settles; the second-activation probe there would read
  the prompt's refusal, not the limit's. An asking payload is probed in
  the test after the answer, inline.
- **A QUOTED LOYALTY LINE IS INDEXED BY ITS SCRUBBED TEXT** (D475). The
  leftover reports `−8: You get an emblem with` with the quote blanked;
  the lines probe matches an activated ability by the scrubbed cost and
  effect too, or the row maker never finds the ability's index.
- **THE ANY-DAMAGE HEAD IS TWO DEFS** (D476). Combat damage is
  `CombatDamageDealt`, the rest `DamageDealt`, and the bus dispatches a def
  on its one event: a `Whenever ~ deals damage` def on `DamageDealt` alone
  never sees an attack. The generator doubles it (`dealsDamage` +
  `dealsDamageAny`) as it doubles `dealsDamagePlayer` (D371).
- **THE MEMO IS THE HEAD'S RECIPIENT'S DAMAGE** (D476). A trampler's damage
  to its blocker is not `combat damage to a player`; the def's `memo` sums
  the source's damage to the head's recipient alone (a player, an opponent,
  a creature, anything for the any-damage head), never the whole batch.
- **A `that much` PAYLOAD READS ONLY UNDER A HEAD THAT MEMOISES** (D476).
  `HEAD_MEMO` is the caller's flag, as `SPELL_X` is: the wide probe passes
  it for a `that much` payload, the row maker admits it under a damage head
  on a creature with power alone, the token-pin guard reads the call site's
  `{ memo: true }`. Without the flag the sentence is refused as before.
- **AN `else` FOLLOWS ITS OWN `if`** (D476). A line inserted between an `if`
  and its `else` re-parents the `else`: D433's p2 line did, and the discard
  queue asserted the wrong player for 43 decisions until a controller's own
  scoped discard came through (April, Reporter of the Weird). Regenerating
  the D472–D475 waves under the repaired generator changed no byte.
- **A GRANTED DEF'S FIRING CARRIES WHAT THE PRESENT DEF'S DOES** (D477).
  D368's granted-trigger path stamped `item` and `player` onto the pending
  trigger and stopped there; a hook added to `TriggerDef` (D476's `memo`)
  reaches a granted def only when BOTH paths stamp it - a granted `Whenever
  you gain life, put that many +1/+1 counters on this creature` (Sunbond)
  read nothing until the second site was written.
- **THE GOBLIN'S BLOCK CANNOT DAMAGE A DEFENDER OR A FLIER** (D477). The
  is-dealt-combat-damage arm attacks into the opponent's 1/1 Goblin; a
  defender cannot attack and the Goblin cannot block a flier, so the suite
  walked the game to its end waiting for a blockers prompt. Those rows take
  their combat damage BLOCKING the Cyclops (5/2) on the opponent's turn -
  dealt 5, and the `killed` baseline reads 5 there.
- **THE ARM'S NUMBER IS THE HEAD'S, NOT THE CARD'S** (D477). D476 sized a
  memo by the printed power (the creature's own unblocked attack); a head
  whose event is not that attack has its own number per arm - 1 or 5 under
  combat damage taken, 3 under noncombat (the Bolt), 3 under the life arms,
  2 under another creature's damage (the Bears), 2 + the attached static's
  power under the attached heads.
- **THE WIDE LOG TRUNCATES A PAYLOAD AT 77 CHARACTERS** (D478). A family
  classified from the row maker's log is classified by its prefix - the
  search residue read as "the grammar lacks the destination" three times
  until a probe parsed the leftover's lines against the live parser and
  found 98 of 345 reading already. Measure a payload family with a probe.
- **A PROBE'S ASK SET AND THE ROW MAKER'S ARE ONE LIST** (D478). The vocab
  probe marks a payload `asks` off its own set; the row maker admits an
  asking payload only when its last effect is in ITS `ASK_KINDS`. A kind in
  the probe's set and not the row maker's refuses every row of that kind,
  silently, as `effect not a row kind` - `search` was, from D390 to D477,
  with the generator answering searches since D359. When a kind joins one
  list it joins both, or the generator's `case` says why not.
- **THE CYCLING ABILITY SITS UNDER ITS OWN TRIGGER** (D478). `When you cycle
  this card` fires with the cycling draw still on the stack beneath it, so a
  settle after the trigger resolves answers whatever the trigger asked (the
  harness declines a search) before the suite can. An asking payload under
  the cycle head is refused until the cycle arm answers the ask itself.
- **A CONDITION ON A STATIC IS PROVEN WITH IT BROKEN** (D388, D381). A suite
  reads the subject at its printed base with the condition unmet - broken FIRST
  where the armed board already satisfies it (untapped, no untapped lands, a
  hand size) - then meets it and reads the static. A condition the card itself
  satisfies (an artifact creature under "you control an artifact") has no unmet
  stage and its title says so.
- **A COUNTER THE STAGE PUTS MOVES P/T ON ITS OWN** (D388). "As long as it has a
  +1/+1 counter on it, it gets +1/+0": the counter is layer 7d, so the met read
  is base + the counter + the static, and a -1/-1 counter on a 7/2 reads 6/1
  with the static absent. When the fixture IS the count, it moves what it
  counts (D387's corollary).
- **A CREATURE THAT CANNOT BE BLOCKED NEVER SEES A DECLARE-BLOCKERS PROMPT, AND
  THE STATIC UNDER PROOF MAY BE WHAT MAKES IT ONE** (D388, D232/D234). "As long
  as it's attacking, it has flying": waiting for the prompt runs the combat out
  and reads the static gone. Read the moment the attack is declared, with
  combat live and no prompt asked for.
- **A STATIC'S CONDITION HELPER IS DERIVE-FREE** (D388, D317). It is asked
  INSIDE a derive, so the engine's activationConditionsHold (which derives)
  cannot be reused; read ctx.state, the PRINTED faces, the turn record, the
  life totals and the live combat.
- **THE STAGE LIVES ON THE ROW: A STAGE FIX IS A RE-ROW** (D388). The row maker
  stores each condition's stage lines beside the descriptor, so regenerating
  from the old rows after fixing the reader reproduces the old suites byte for
  byte. Re-row, then regenerate.
- **COUNT THE BACKSLASHES AGAINST THE ORIGINAL LINE** (D388, invariant 14). A
  node patch that hoists a regex arrives with its doubled backslashes halved, and
  a template literal cooks the lone escape to a bare letter - silently. A swap
  anchor spelled with a doubled backslash-n arrives as a real newline and matches
  nothing. Spell a backslash by String.fromCharCode(92) and compare counts.
- **A COUNTED FIXTURE MUST MOVE THE COUNT AND NOTHING ELSE** (D387). An attached
  count proven with Accorder's Shield or Holy Strength would read their own
  `+0/+3` and `+1/+2` on top of the pump's delta; Swiftfoot Boots (hexproof and
  haste) and Pacifism (can't attack or block) are P/T-neutral. And an Equipment
  that counts "Equipment attached to it" COUNTS ITSELF once equipped - the
  starting count is 1, not 0, and the generator computes it.
- **A BLOCKED ATTACKER MAY DIE IN THE SAME COMBAT - PROVE THE COUNT ACROSS
  GAMES, NOT TURNS** (D387). A becomes-blocked pump is read right after the
  trigger resolves in the declare-blockers step, in one game blocked by one
  creature and another blocked by two; a second attack next turn presumes the
  attacker survived the first. `advanceUntil(... awaiting?.kind ===
  'declareBlockers')` stops ON the prompt because the prompt is the predicate
  (D370's trap is a predicate that holds without it), and the defender declares
  the blocks by hand.
- **A NEGATIVE PUMP GETS ONE STEP** (D387, D323). `-1/-1` per counted thing on a
  2/2 host: one step reads 1/1, a second reads 0/0 and the SBA bins the host
  before the assert. One counted in, one counted out, on a host that survives.
- **A PRICER'S REGEX IS A MEASUREMENT AND IT CAN HIDE HALF THE FAMILY** (D387).
  The variable-pump pricer required a `+` before the power, so every negative
  pump was invisible to the count that chose the widenings - twelve cards.
  Price with the sign the GENERATOR reads, and re-price after widening the
  reader: 0 -> 33 landable was the measurement, not 21.
- **A TEST AT THE EDGE OF ITS BUDGET FAILS UNDER THE SHARDED GATE'S OWN LOAD**
  (D387, D269). The fuzz rewind test runs in EVERY shard at once; at 18.7 s
  idle it read 25.8-30.6 s in four of six shards against the 20 s default,
  every assertion green. Measure it ALONE first (a timeout with green asserts
  is a budget, not a hang), then give it an explicit budget - the sibling
  projection-leak test carries 60 s - and rerun the gate from the top.
- **A DERIVE'S OWN REPLACE TABLE IS NOT SAFE FROM THE SED THAT MAKES IT**
  (D387, D378 again). `sed 's/386/387/'` over a derive whose table contains
  `['d386', 'd387']` produces `['d387', 'd386']`. Write the next derive by hand
  with an explicit take-list.
- **ONE PASSING LEG IS A SAMPLE, NOT A FLOOR** (D386, correcting D385; D155/D176).
  D385 made the static-prevention canary's floor unconditional because one
  60-seed leg passed it. D386's 21 new scripted names shifted every seed's
  round-robin window (D193) and the same 60 seeds read ZERO, while 500 read 77
  with every shard positive. A floor is set where the fuel is RELIABLE - measured
  more than once at the size it asserts - and softening it back to gate size on
  that evidence is a correction, not the weakening D385's own fallback note
  warned against (which is softening with NO new measurement to pass a red gate).
- **A CONDITION IS PROVEN BY THE FIRE THAT DOES NOT HAPPEN** (D381). Firing once
  with a condition MET proves the ability resolves and says nothing about the
  condition. Fire twice - broken, then met - with the stage-one check as a THROW
  inside the arm, where the mark is in hand; and refuse a payload the arm cannot
  read rather than widening the read.
- **A FIXTURE DERIVATION MUST ANSWER EVERY FIELD THE FILTER CARRIES** (D381). One
  that ignores a field hands the suite a card that does not satisfy it - a
  while-you-control-a-TOKEN condition was given a Badlands. D350 rule: a filter
  no fixture satisfies REFUSES the row, and is never given one that ignores it.
- **A NAME THAT IS A RULES WORD** (D373). `selfRef` spells a card own name
  `~` before any rule runs, so the card named Regenerate arrives as "~ target
  creature." and reads as nothing. Ledger it under THAT, not under the
  mechanic it appears to be waiting for.
- **THE UNTAP SYMBOL IS A COST AND ITS OFFER IS GATED ON BEING TAPPED**
  (D373). `legal.ts` skips a `requiresUntap` ability while the permanent is
  UNTAPPED, so a `{Q}` row is not offered at all until the scaffold taps the
  recipient - which is also the only way an untap PAYLOAD is observable
  (D128). The generator taps first in both cases.

## Open reportables you inherit

0. ⚠️⚠️ **`select.cjs` EMITS SPELLS THE PIPELINE CANNOT LAND** (D160). A spell
   executes through the effect vocabulary or not at all — `CardScript` has no
   spell seam — yet vocabulary-refused instants/sorceries are sole-need
   `scriptable` and re-appear in EVERY batch (`Aliban's Tower` is the first).
   Decide before the next batch: filter spells out of the selection, or queue
   them for vocabulary work. Also from D160: a script cannot raise a PROMPT
   from `resolve` (blocks the `Abyssal Horror` class), and the "modified"
   predicate awaits its own decision.
1. ⚠️ **The ~800 def-gated sacrifice-self cards are the widest seam open**
   (D159). `Sacrifice this <type>` is a chargeable price now, offered only
   when the game's registry carries the `ActivatedDef` — so each of those
   cards is ONE DEF away from executing, on machinery two landed cards
   already prove. A general sacrifice cost ("Sacrifice a creature") is a
   CHOOSER the activation stage does not have, and stays unpaid.
2. **`ctx.random` is a STUB at the (single, since D159) `ScriptCtx`
   construction site** (`below: () => 0`, `shuffled: (xs) => xs`) while
   `api.ts`'s header promises a seeded scratch RNG threaded through the log.
   No script that needs randomness may ship until it is wired.
3. **Targeting aims with the FRONT face's specs.** `beginAim` calls
   `session.targetSpecsFor(card)` with no face index, so a modal-DFC back face
   that targets cannot be cast through the UI. It fails **safe** — the host
   validates against the real face (D139) and rejects — and the fix is one
   parameter. Affects the subset of the 38 spell-back-face MDFCs that target.
4. **`collectTriggers` scans every card id per (event × def).** Fine at
   382.8 s with 22 defs (D159); the first suspect when the library grows. A
   per-oracle-id source index preserves iteration order bit-for-bit — build it
   measured, on an idle machine (D106).
5. **Computed life costs beyond War Room's exact phrase stay unpaid** — each
   new phrase is its own D90-anchored recognition with its own computation
   (D159's `lifeCostCommanderColors` is the template).
   ⚠️ Standing review rule from D158/D159: a `TriggerDef.abilityId` matching
   `/^a\d+$/` collides with the parsed-activated `abilityRef` namespace.

## When you finish a batch

Run the gates, then write a `docs/DECISIONS.md` entry (**the next number is
D198**) and an `AGENTS.md`
milestone entry, in this repo's style: what was
measured, what was built, what broke and why, **what you checked by BREAKING
it**, and the numbers.

⚠️ **Report coverage as a measured number every batch** — *"X of 31,692
Commander-legal cards now execute completely, up from Y"* — never as a count of
scripts written. Coverage is asymptotic and the last few hundred cards will cost
more than the first ten thousand. Say the number honestly, including when it
moves less than hoped.
