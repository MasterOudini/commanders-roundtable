// `Network Disruptor` — "When this creature enters, tap target permanent."
// Chrome Prowler's targeted ETB tap widened to any permanent. D228.

import { NETWORK_DISRUPTOR } from '../../../data/fixtures/engineCards';
import { parseTargetClauses } from '../../../data/targetParse';
import type { CardData } from '../../../data/cardTypes';
import type { CardScript } from '../api';
import type { EventBody } from '../../types/events';

function printed(card: CardData, expected: string): string {
  const actual = card.faces[0]?.oracleText;
  if (actual !== expected) {
    throw new Error(
      `${card.name} reads "${actual}" and its script was written for "${expected}". ` +
        'Re-read the card before re-registering it (D90).',
    );
  }
  return expected;
}

const PRINTED = printed(
  NETWORK_DISRUPTOR,
  'Flying\nWhen this creature enters, tap target permanent.',
);
const TEXT = PRINTED.split('\n')[1] as string;

export const NETWORK_DISRUPTOR_SCRIPT: CardScript = {
  oracleId: NETWORK_DISRUPTOR.oracleId,
  name: NETWORK_DISRUPTOR.name,
  triggers: [
    {
      abilityId: 'etb-tap',
      text: TEXT,
      event: 'CardsMoved',
      activeZones: ['battlefield'],
      optional: false,
      targets: parseTargetClauses(TEXT),
      matches: (_ctx, self, ev) =>
        ev.t === 'CardsMoved' &&
        ev.moves.some(
          (m) => m.card === self && m.to.kind === 'battlefield' && m.from.kind !== 'battlefield',
        ),
      label: () => 'Network Disruptor — tap target permanent',
      resolve: (ctx, _self, obj): readonly EventBody[] => {
        const target = obj.targets[0];
        if (!target || target.kind !== 'card') return [];
        const card = ctx.state.cards[target.id];
        if (!card || card.zone.kind !== 'battlefield' || card.tapped) return [];
        return [{ t: 'PermanentsTapped', cards: [target.id] }];
      },
    },
  ],
};
