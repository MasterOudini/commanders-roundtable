// `Bant Battlemage` — "{G}, {T}: Target creature gains trample until end of turn.\n{U}, {T}: Target creature gains flying until end of turn."
// Two tap abilities on one Wizard, each a Caller of Gales (D202) / Wyluli
// Wolf (D271) pump-or-grant on the tap carrier. GENERATED by
// gen-guildmages.cjs — edit the table, never this file. D272.

import { BANT_BATTLEMAGE } from '../../../data/fixtures/engineCards';
import type { CardData } from '../../../data/cardTypes';
import type { CardScript } from '../api';
import type { EventBody } from '../../types/events';

function printed(card: CardData, expected: string): string {
  const actual = card.faces[0]?.oracleText;
  if (actual !== expected) {
    throw new Error(
      card.name + ' reads "' + actual + '" and its script was written for "' + expected + '". ' +
        'Re-read the card before re-registering it (D90).',
    );
  }
  return expected;
}

const PRINTED = printed(
  BANT_BATTLEMAGE,
  '{G}, {T}: Target creature gains trample until end of turn.\n{U}, {T}: Target creature gains flying until end of turn.',
);
const LINES = PRINTED.split('\n');

export const BANT_BATTLEMAGE_SCRIPT: CardScript = {
  oracleId: BANT_BATTLEMAGE.oracleId,
  name: BANT_BATTLEMAGE.name,
  activated: [
    {
      ref: `${BANT_BATTLEMAGE.oracleId}#a0`,
      text: LINES[0] as string,
      resolve: (ctx, _self, obj): readonly EventBody[] => {
        const target = obj.targets[0];
        if (!target || target.kind !== 'card') return [];
        if (ctx.state.cards[target.id]?.zone.kind !== 'battlefield') return [];
        return [
          {
            t: 'PtModifiedUntilEndOfTurn',
            card: target.id,
            power: 0,
            toughness: 0,
            keywords: ['trample'],
          },
        ];
      },
    },
    {
      ref: `${BANT_BATTLEMAGE.oracleId}#a1`,
      text: LINES[1] as string,
      resolve: (ctx, _self, obj): readonly EventBody[] => {
        const target = obj.targets[0];
        if (!target || target.kind !== 'card') return [];
        if (ctx.state.cards[target.id]?.zone.kind !== 'battlefield') return [];
        return [
          {
            t: 'PtModifiedUntilEndOfTurn',
            card: target.id,
            power: 0,
            toughness: 0,
            keywords: ['flying'],
          },
        ];
      },
    },
  ],
};
